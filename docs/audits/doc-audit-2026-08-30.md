# Doc audit — 2026-08-30

Audited at commit: `e92c0b552cc65234f39553abc7690c71d20dd8a1` (`e92c0b5`), working tree clean.

## Artefacts

**Tier 1 — agent-facing (CLAUDE.md / AGENTS.md): NONE.**
Confirmed absent by the absence protocol: `find . -iname "CLAUDE.md" -o -iname "AGENTS.md"` → no output;
`git ls-files | grep -iE '(CLAUDE|AGENTS)\.md'` → no match; positive control `find . -iname "README.md"` →
`./README.md`, `git ls-files | wc -l` → 46. **No P0 finding is possible in this repo** — the severity class
is defined as a false claim re-injected into every session, and no such surface exists here.

**Tier 2 (12):** `CONTEXT.md`, `docs/adr/0001`–`docs/adr/0011`.

**Tier 3 (1):** `README.md`.

**Additional self-describing docs audited** (F3's nearest analogues, since Tier 1 is empty — these make
factual claims about this repo's own artefacts and are read by agents picking the work up):
`THREAD.md`, `docs/build-plan.md`, `docs/windows-setup.md`, `skills/_shared/execution-fit.md`,
`skills/_shared/task-writer.md`, `skills/execute/subagent-prompt-template.md`.

**Code surface used as arbiter:** `skills/*/SKILL.md` (13), `skills/schedule/rollout-template.md`,
`skills/execute/wave-execute.workflow.js`, `skills/execute/scripts/{merge-wave.sh,reconcile-wave.py}`,
`skills/execute/tests/*`, `hooks/{hooks.json,wave-stop-driver.py}`,
`.claude-plugin/{plugin.json,marketplace.json}`, `git log`.

**Out of scope, deliberately:** `docs/wave-THREAD-archive.md` — labelled "wave's full build history,
verbatim" at `README.md:71`, an explicit archive. No findings reported inside it.

---

## Findings

### Significant

---

**S1 — `THREAD.md:229` (with `:155-156`): "the two ADRs" — eleven exist.**
Family F3 · ssot **doc-wrong** · confidence high

**Claim:** `THREAD.md:229` — "1. Read this file, then `CONTEXT.md` and the two ADRs."
Corroborating stale surface at `:155-156`, which lists only ADR 0001 and ADR 0002 under
"## What's been built / decided".

**Code evidence:** `ls docs/adr/ | wc -l` → **11** (0001–0011).
`git log -L 227,234:THREAD.md --oneline` → exactly one commit, `6c0258b ✨ feat: thread plugin v1.0.0`,
with the diff shown as file creation — the `## Resume instructions` block is verbatim v1.0.0 text, never
edited since. `git ls-tree -r --name-only 6c0258b -- docs/adr/` → exactly 2 files, so the claim was true
when written and is false now. The heading at `:227` carries no date or version qualifier, so the
"scoped to a v1.0.0 section" defence fails.
Per-ADR add dates: 0001+0002 = 2026-07-14; 0003 = 2026-08-08; 0004–0009 = 2026-08-12; 0010 = 2026-08-14;
0011 = 2026-08-29.

**Harm:** an agent obeying line 229 reads 2 of 11 ADRs and skips **0009** (thread absorbs wave — the entire
rollout lane, 7 of the 13 skills), **0010** (orient self-launches batches), and **0011** (close saves
autonomously, gates only vault tasks — the decision governing current close behaviour).

**Suggested fix:** point step 1 at `docs/adr/` as a directory, and refresh `:155-156` in the same edit —
they are one staleness with two surfaces.

---

**S2 — `docs/build-plan.md` is a frozen pre-build plan carrying no historical marker.**
Family F7 · ssot **doc-wrong** · confidence high

**Claim:** the whole 269-line file, headed `# Plan: `thread:*` — a continuity family (router + intentional
routes)`, reads as a live plan.

**Code evidence:** `git log --follow -- docs/build-plan.md` → a single commit, `fa6173c`, 2026-07-14
14:47:07, never modified since. `grep -inE 'archive|historical|superseded|lineage|as-built|record of'
docs/build-plan.md` → **exit 1, zero matches** (positive control: same grep on `THREAD.md` → 5 hits). No
frontmatter, no date stamp, no disclaimer, no HTML comment. Not archive-named, and absent from the repo's
only doc index — `grep -n 'docs/' README.md` → lines 65, 67, 70, 71 only, none of them this file.

The repo demonstrably knows how to label frozen history and did not do it here:
`docs/wave-THREAD-archive.md` is archive-named **and** labelled at `README.md:71`. `docs/build-plan.md`'s
only label lives in a different file, `THREAD.md:168`.

**Drift inventory inside the file** (these fold in as evidence, not as separate findings):
- `:26-33` a six-member skill table vs 13 shipped (`ls -d skills/*/ | grep -v _shared | wc -l` → 13).
- `:97-112` a file tree with no `hooks/`, no `skills/execute/`, no rollout skills, no
  `docs/wave-THREAD-archive.md` — all present.
- `:134-163` `## Migration`, an imperative checklist for a reference sweep completed 2026-07-14.
- `:230-238` proposes ADRs 0001 and 0002 as "candidates to offer at build"; eleven are accepted.
- `:256-263` proposes a `/weekly` Stashed-threads section that has shipped
  (`ops-workspace/.claude/commands/weekly.md:111`).
- `:86` a live-voice `/wave:schedule` reference; the namespace was hard-cut at 2.0.0 (`e491076`;
  `docs/adr/0009:34` "no `/wave:*` stubs"). This is the sole `/wave:` occurrence outside the archive, the
  ADRs, and `THREAD.md:129`'s rename note.
- `:138` "`thread:handoff` keeps `disable-model-invocation: true`" — dropped in `c4ae369`.
- `:203-228` a glossary self-declared as seeding CONTEXT.md "**verbatim** at build" (`:206`) that has since
  diverged from it in at least three ways (Task floor lacks CONTEXT.md's "shared atom of both ladders"
  sentence; THREAD.md paths are relative not absolute; Handoff lacks the 2.0.1 model-invocability rule) —
  the file's own promise is now falsified by its own contents.

**Suggested fix:** one banner at the top ("historical: the approved 2026-07-14 build plan; the live glossary
is `CONTEXT.md`, the live decisions are `docs/adr/`"), or rename it beside `wave-THREAD-archive.md`. This
single edit discharges every fold-in above. Do **not** retro-edit the plan's Context section — a plan's
"Today three unrelated flat skills half-cover this" (`:13`, inside `## Context — why`, which closes at
`:19`) is the genre working correctly.

---

**S3 — `docs/build-plan.md:177-181` is a live-routed acceptance test that green-lights a spec violation.**
Family F3 · ssot **doc-wrong** · confidence high

**Claim:** `:181` — "`thread:defer` → task at `Work/Tasks/<slug>.md` with `scheduled: <tomorrow>`, correct
`projects:`, self-contained body; confirm it appears in the TaskNotes agenda and via
`rg \"^scheduled: <tomorrow>\" Work/Tasks/`."

**Code evidence:** the current spec does not treat these as partial coverage — it **demotes** them and
**omits** the step it mandates.
`skills/_shared/task-writer.md:56` `## 3b. Make it visible on the day page (defer only)`; `:58-59`
"`scheduled:` frontmatter alone is INVISIBLE — the day note's `## To do` checklist is the surface Lachy
actually works from (2026-07-30 feedback: a deferred task he could not see on the Monday page)"; `:68`
mandates appending `- [ ] [[<slug>|<title>]]`.
`skills/defer/SKILL.md:20` — "**The day page is the visible surface — frontmatter alone is not enough.**
Always run task-writer § 3b … `/morning`'s `rg \"^scheduled: $TODAY\"` and the TaskNotes agenda are
**secondary discovery, not the surface Lachy sees**."
`git log -1 984fc0d` → 2026-07-30 "feat: defer writes a visible To do line on the target day page (v1.2.0)"
— **postdates** build-plan.md's only commit (2026-07-14).

A defer that writes `scheduled:` and no To do line **passes** this checklist and **fails** the current spec.

**Routing is live, not hypothetical.** `THREAD.md:231-233` § Resume instructions names "the plan's
Verification section" as the live testing checklist, and its cited source
(`~/.claude/plans/so-i-want-to-rippling-taco.md`) no longer exists — leaving `docs/build-plan.md` the only
reachable copy. The route has already been walked: an archived vault task
(`Work/Tasks/Archive/thread-plugin-live-verification.md:15`) copied this checklist from this file verbatim
and was completed 2026-08-27, a month after `984fc0d` made the To do line load-bearing, with no To do line
check in it.

**Why this is separate from S2:** stamping build-plan.md historical does not fix it — it converts
`THREAD.md:233` into an active contradiction. The fix must touch `THREAD.md:231-233` or the checklist
content.

**Suggested fix:** repoint `THREAD.md:231-233` at the real test suite (`README.md:91-101`) and either drop
the § Verification checklist or add the § 3b day-page assertion to it.

### Moderate

---

**M1 — `THREAD.md:187` states a dropped flag as a current fact, under "don't re-derive".**
Family F3 · ssot **doc-wrong** · confidence high

**Claim:** "`disable-model-invocation: true` hides a skill from the model's list but keeps the `/slash`
form — **used by `thread:handoff`**."

**Code evidence:** the first clause is TRUE and load-bearing — `skills/execute/SKILL.md:335` and
`docs/wave-THREAD-archive.md:319-321` both rely on it as a "never add this flag" warning. Only the trailing
attribution is dead. No `SKILL.md` in the repo carries the key (loop over `skills/*/SKILL.md`, `head -12` →
zero hits); `skills/handoff/SKILL.md:1-8` frontmatter is name/description/argument-hint/author/license/source.
`git show c4ae369 -- skills/handoff/SKILL.md` → `-disable-model-invocation: true`, subject "feat(handoff):
model-invocable on explicit fork intent — drop inherited flag".
`skills/handoff/SKILL.md:19` records the reversal. `THREAD.md:117` records it too — seventy lines above the
stale line, so THREAD.md contradicts itself. The heading at `:183` is "## Known quirks (don't re-derive)",
which instructs the reader **not** to verify it, and carries no date or version marker.

**Suggested fix:** delete the four-word trailing clause. Keep the bullet — its mechanism half is a live
warning two other files depend on.

---

**M2 — `README.md:65-70`: the repo's only ADR index stops at 0009.**
Family F6 · ssot **doc-wrong** · confidence high

**Claim:** the `## Design` section enumerates `docs/adr/0001`–`0003`, `0004`–`0008`, `0009` as three
contiguous, gapless ranges with no hedge word.

**Code evidence:** `ls docs/adr/` → eleven files. Absence protocol: `grep -n '0010\|0011' README.md` →
exit 1, no match; `grep -in 'adr' README.md` → only L46, L65, L67, L70; positive control
`grep -n '0009' README.md` → L70 fires. `find . -name 'README*' -not -path ./.git/*` → `./README.md` only,
and `grep -rn 'docs/adr' --include='*.md' .` → only README.md:65,67,70 — **there is no other ADR index**.
Missing: `0010-orient-launches-its-own-batches.md` (`Date: 2026-08-14`, "Status: accepted (supersedes the
launch conclusion of ADR 0003)") and `0011-close-saves-autonomously-vault-tasks-stay-gated.md`
(`Date: 2026-08-29`, accepted).
`git log --date=short -- README.md` → newest `f65ab02` **2026-08-12**; the repo has shipped two ADRs and two
minor versions since without touching it.

**Aggravator:** a reader following this index lands on ADR 0003 — which carries no supersession marker
(see M3) — and is never pointed toward ADR 0010, the ADR that reverses it.

**Suggested fix:** extend the index. Cheaper still, replace the enumeration with a pointer to `docs/adr/`
so it cannot go stale again.

---

**M3 — `docs/adr/0003` has been superseded twice and carries no marker of any kind.**
Family F4 · ssot **doc-wrong** (additive fix only) · confidence high

**Claim:** three surfaces of ADR 0003 still assert the retired model —
`:1` (title) "…and dispatch is an artefact";
`:8-10` "it emits **launch artefacts** (prompt files + scoped-profile terminal one-liners), never launched
sessions";
`:19` "Orient *refers* PR-shaped clusters to `/wave:*` rather than growing a second engine".

**Code evidence:** `grep -niE '^status:|^date:|supersed|deprecat' docs/adr/*.md` →
Status/Date headers exist on **0009, 0010, 0011 only**; 0001–0008 carry no header block at all. ADR 0003's
sole `supersed` hit (`:44`) is about `dispatched:` stamps, unrelated.
Both reversals are recorded — but only on the *superseding* side:
`docs/adr/0009:4` "Status: accepted (supersedes the packaging conclusion of ADR 0003)";
`docs/adr/0010:4` "Status: accepted (supersedes the launch conclusion of ADR 0003)".
Live code agrees with 0010, not 0003: `skills/orient/SKILL.md:131-135` "**Launch the batch as a cmux
workspace** (the steering answer already authorised this — no second confirm, ADR 0010): `cmux workspace
create …`", with `:150-152` demoting emission to "**Fallback — no reachable cmux socket**".
The `/wave:*` namespace does not exist: `grep -rn '/wave:' skills/ hooks/ .claude-plugin/` → zero hits
(positive control `grep -rno '/thread:' skills/ | wc -l` → 178). `skills/orient/SKILL.md:96-101` routes
wave-shaped clusters to intra-plugin `skills/gather/` and `skills/schedule/`.
`git log --oneline -- docs/adr/0003…` → a single commit, never touched since creation.

**Why "superseded ADRs are healthy history" does not refute this:** that rule protects ADRs that *are
marked*. This one is not, and the forward pointers do not rescue it because the README index (M2) stops at
0009 — a reader entering from the README is pointed at 0003 and never toward 0010.

**Suggested fix:** add a `Date:` + `Status:` header to 0003 naming the two partial supersessions (0009
packaging, 0010 launch). **Do not rewrite the body** — `docs/adr/0009:37-40` explicitly preserves 0003's
scope reasoning, so the supersession is genuinely partial and the body is a correct historical record.

### Minor

---

**m1 — `THREAD.md:165`: the vault project note moved.** F1 · doc-wrong · high
Claim: "Vault project note `Work/Projects/Side projects/Thread Skill.md` is the human-facing surface".
Evidence: in `~/repos/obsidian`, `find . -name 'Thread Skill*'` → `./Work/Projects/AI/Thread Skill.md`
(single hit); `ls -d 'Work/Projects/Side projects'` → No such file or directory; the whole folder is gone
(`git log --follow --diff-filter=R` → `06b27f05 ♻️ vault: new AI area — agent notes consolidated`).
Classified `moved`, not `missing`. Two mitigations: the load-bearing half of the sentence is still true —
the moved note's frontmatter carries `repos: - ~/repos/tools/thread-skill` intact, so the CWD auto-routing
it promises works; and nothing else in the vault references the old path.
**Fix:** one-token path correction to `Work/Projects/AI/Thread Skill.md`.

---

**m2 — `THREAD.md:233`: dead plan-file pointer.** F1 · doc-wrong · high
Claim: "plan file: `~/.claude/plans/so-i-want-to-rippling-taco.md`, mirrored in repo history".
Evidence: `ls ~/.claude/plans/so-i-want-to-rippling-taco.md` → No such file or directory; positive control
`ls ~/.claude/plans/ | wc -l` → 207; `find ~/.claude -iname '*rippling*' -o -iname '*taco*'` → no output
(not a rename). The "mirrored in repo history" fallback resolves to `docs/build-plan.md`, which exists and
is independently named two sections earlier at `THREAD.md:168`, so a resuming agent is not stranded.
**Fix:** delete the dead path, point at `docs/build-plan.md` directly. (Do not write new verification
coverage for the 7 uncovered skills — they post-date the v1.0.0 plan; thin coverage is expected there,
not drift. The checklist's *content* problem is S3.)

---

**m3 — `README.md:22`: the orient row describes the fallback as the default.** F6 · doc-wrong · high
Claim: "…route by the execution-fit test — wave-shaped clusters to `gather`/`schedule`, the rest to `open`
or **batch dispatch artefacts**."
Evidence: not false — "dispatch artefact" survives ADR 0010 (the prompt file is still the batch's durable
contract) and artefacts-only is still a real documented code path (the no-cmux fallback). But the row omits
the entire point of ADR 0010. `grep -in 'launch\|cmux\|batch' README.md` → only L22 and L111 (Orca section,
unrelated): **README nowhere mentions that orient spawns sessions**, and it spawns them
under `--dangerously-skip-permissions` with the steering answer as the sole authorisation
(`skills/orient/SKILL.md:14-16`, `:77-80`; `docs/adr/0010` § Rejected: "Keep emitted-never-launched").
Chronology kills the "deliberate compression" defence: `git log -1 -S'batch dispatch artefacts' -- README.md`
→ `f65ab02` 2026-08-12; `skills/orient/SKILL.md` gained the launch behaviour in `d6cece5` 2026-08-14, which
did not touch README.md. Same root cause as M2 — README last written 2026-08-12.
**Fix:** rewrite the row's tail to say orient launches its batch sessions itself. Consider Moderate if you
weight "omits an autonomous, permission-skipping side effect" heavily; on the text alone it is Minor.

---

**m4 — `README.md:54-55`: `scripts/…` paths do not resolve from the repo root.** F1 · doc-wrong · high
Claim: "Merges go **only** through `scripts/merge-wave.sh` … `scripts/reconcile-wave.py` writes all per-task
vault frontmatter transitions…"
Evidence: `find . -type d -name scripts -not -path ./.git/*` → `./skills/execute/scripts` only; there is no
root `./scripts`. The relative-to-`skills/execute/` reading fails: **every other path in README.md is
repo-root-relative**, including one twelve words earlier in the same paragraph
(`README.md:41` `skills/execute/wave-execute.workflow.js`), and README's own Tests block writes the full
paths forty lines later (`:95`, `:97`). These two are the file's only exceptions.
The substantive claims all verify (`merge-wave.sh:15` "NEVER uses `--admin`"; `gh pr merge` appears only
inside `merge-wave.sh:285`; no force-push anywhere).
**Fix:** two-token path correction.

---

**m5 — `skills/repair/SKILL.md:3, :16, :134`: "wave keeps sole merge authority".** F5 · ssot **code-wrong** · high
Claim (in code, contradicting the glossary): repair's own prose treats "wave" as an agent holding authority.
Evidence: `CONTEXT.md:110` carries exactly one Wave definition — "a set of tasks within a rollout" — under
which a wave cannot hold merge authority. `CONTEXT.md:174` and `README.md:37` both say "**the engine** keeps
sole merge authority". Git provenance settles the referent: `git log -S 'wave keeps sole merge authority' --
skills/repair/SKILL.md` → `1ebbc11` (authored while `wave` was still the plugin namespace), whereas
`git log -S 'the engine keeps sole merge authority' -- README.md` → `f65ab02`, the ADR 0009 cutover commit.
The cutover updated README and CONTEXT.md to the new noun and left repair at the old one. `docs/adr/0009:32-33`
grandfathers only *identifiers* (`wave:` frontmatter, `WAVE-STATUS:` internals), not prose.
Zero behavioural risk — the operative instruction in the same bullet (`:133` "Don't merge anywhere but
`merge-wave.sh`. No inline `gh pr merge`, no `--admin`, no force-push") is mechanically unambiguous.
**Fix:** reword the three occurrences to "the engine". This is the one finding where the **code** is the side
that yields.

---

**m6 — `skills/execute/subagent-prompt-template.md`: stale builder table and a wrong count.** F4 · doc-wrong · high
Claim: the doc's variant→builder table lists pre-escalation signatures; the `tier`/`prior` parameters that
*are* the ADR 0006 escalation mechanism are missing from 6 of 8 rows.
Evidence (`grep -n 'function ' skills/execute/wave-execute.workflow.js`, exact):
`:390 implementerPrompt(task, a, tier, prior)` vs doc `:13 (task, a)`;
`:417 readOnlyPrompt(task, a, prior)` vs doc `:14 (task, a)`;
`:436 plannerPrompt(task, a, prior)` vs doc `:15 (task, a)`;
`:530 approvedPlanImplementerPrompt(task, planText, a, tier, prior)` vs doc `:18 (task, plan, a)`;
`:563 reviewJudgePrompt(task, prevImpl, a, priorFeedback)` vs doc `:19 (task, prevImpl, a)`;
`:586 reviserPrompt(task, prevImpl, priorFeedback, round, a, planText)` vs doc
`:20 (task, prevImpl, feedback, round, a)`.
**Partly mitigated:** the doc is a tombstone ("# Per-task subagent prompts — **moved**"; `:3` "no longer read
from this file at runtime"), its framing sentence at `:9` is "The variants and the functions that build
them:", the column header is "Builder", and it never reproduces parameter names verbatim anywhere — even the
two "matching" rows match on arity only. Zero behavioural risk (module-internal functions, no external
caller), and `:26` says "To change a prompt, edit the builder in the workflow script".
**Better anchor, same file:** `:3` says "the **five** prompt variants now live as inlined template-builder
functions" while the table below lists **eight** rows — a flat count error immune to the paraphrase defence.
**Fix:** correct the count at `:3`; optionally refresh the table or drop its parameter lists entirely.

---

**m7 — `skills/_shared/execution-fit.md:34` cites the wrong task-writer section.** F3 · doc-wrong · high
Claim: "It is conversation-gated: it needs Lachy's input before an agent can act (task-writer **§ 4** notes
this in the body)."
Evidence: `grep -n '^#' skills/_shared/task-writer.md` → `:75 ## 4. Task file shape`, `:107 ## 5. Body —
everything lives inside`, `:155 ## 6. …` — the file numbers its own sections. § 4 spans 75–106: frontmatter,
slug rule, `launch:` alias, `thread` marker tag, typed values. Nothing about conversation-gating. The guard
is at `:151-153`, i.e. § 5 — and the citation's own wording indicts it, since § 5's title is literally
"**Body** — everything lives inside".
Outlier confirmed: `orient/SKILL.md:167` cites "§ 5" correctly for the same guard;
`defer/SKILL.md:13` → § 3 ✓; `defer/SKILL.md:20` → § 3b ✓; `stash/SKILL.md:22` → § 1.4 ✓;
`close/SKILL.md:50` → §§ 1 & 4 ✓. Five of six citations are right; this is the sole miscitation.
**Fix:** one word, `§ 4` → `§ 5`.

---

## Duplicated truth

**D1 — the per-role EFFORT matrix has three homes, two of which assert it has one.**
Family F8 · all copies currently **agree** — this is latent, not present drift.

- **Canonical:** `skills/execute/wave-execute.workflow.js:713-716`
  `const EFFORT = { opus: {implementer:'medium', judge:'high', masterReview:'high', reconcile:'low'},
  fable: {implementer:'high', judge:'high', masterReview:'xhigh', reconcile:'low'} }`
- `skills/execute/SKILL.md:69-77` — reproduces it as a four-row markdown table **immediately after**
  asserting "The per-role matrix is fixed in ONE place in the engine (`wave-execute.workflow.js`, the
  `EFFORT` constant)".
- `skills/schedule/SKILL.md:209` — reproduces the full matrix in prose, then says "The matrix is fixed in
  the engine (see execute SKILL.md § Effort bundles)".

Both docs also state the tuning rule that guarantees the drift: "Tuning the matrix means editing the engine."

**`docs/adr/0007:4-7` is NOT part of this finding.** An ADR restating the values it *decided* is the record
working correctly, and `:34-35`'s "the per-role matrix lives in one place in `wave-execute.workflow.js`" is
a claim about code structure (one constant, not literals scattered per spawn site), not a prohibition on the
ADR restating its own decision. ADRs are immutable point-in-time records; a future tuning supersedes 0007
rather than falsifying it.

**Model to follow:** `CONTEXT.md:181-187` already does this correctly — it defines the tier concept
(model **and** effort, moving together, `effort:` as the single escape hatch) and reproduces **no values**.

**Fix:** `execute/SKILL.md` and `schedule/SKILL.md` state the *shape* of the bundle and the stance, and cite
the constant for the values. Only the constant carries numbers.

---

## Unable to verify

Listed so silence is not mistaken for a clean result.

- **`README.md:107-118` "Coexistence with Orca"** — Orca's worktree auto-discovery, its `external` labelling,
  and `https://www.onorca.dev/` are properties of an external product this repo cannot observe. (The
  `audit-fix/*` branch-name ownership *was* verified in-repo: `wave-execute.workflow.js:657`
  `` const br = `audit-fix/${shortAlias(task.slug)}` ``. The "11am `daily-git-sweep` reaper only removes
  clean AND merged worktrees" claim was cross-checked against the external
  `com.lachy.daily-git-sweep` plist + `prune_worktrees()` and found **correct**, but it describes an
  external system and is not this repo's to assert.)
- **`README.md:75-80` Install, HTTPS-marketplace form** — `gh repo view lachyts/thread-skill` reports
  `isPrivate: true`, and `docs/windows-setup.md:7` lists "`gh` CLI authenticated as `lachyts` (this repo is
  private)" as a prerequisite the README omits. Could not demonstrate the README form fails on an
  authenticated machine, so it stays out of the findings — flagged as a possible completeness gap.
- **`CONTEXT.md` § "Memory capture (close-side)"** (Four-verb triage, Provisional, Provenance, Recall,
  Curator, Permanent, Demotion) — `CONTEXT.md:83` says the Curator "Lives outside this repo", confirmed
  (launchd `com.lachy.memory-curator` in `~/repos/workspaces/`). Not self-describing; no code in this repo
  can arbitrate these definitions.
- **Behavioural test suites not executed** — `skills/execute/tests/reconcile-wave.test.sh` and
  `tests/wave-stop-driver.test.sh` were syntax-checked (`bash -n`) but not run, on side-effect grounds.
  `merge-wave.sh --self-test-classify` was run (pure classifier, 12 assertions, exit 0).
- **`THREAD.md` § Open questions (Windows items)** — checked and genuinely open, not drift: the last four
  commits are 2.2.0 work that never touched Windows.
- **`THREAD.md:96-99`** `thread-schedule-package-init-fileset-blindness` "remaining from the audit" — checked
  whether it had quietly shipped (greps for `fileset` / `__init__` / package-init across md/js/py/sh;
  positive control `file-overlap` → README.md:34, schedule/SKILL.md:215-222, repair/SKILL.md:103-111). No fix
  exists and no schedule commit post-dates 2.0.3 — **the claim is accurate and the item is still open.**

---

## Checks run

Method note: the skill specifies the Workflow tool for the Phase 2 fan-out. Workflow was unavailable in this
execution context, so the fan-out ran as six parallel `Agent` finders and five parallel independent refuters,
all pinned to **Opus** (the standing rule for audit fan-outs). Phase 3 refutation was run by agents with
fresh context that did not author the candidates. This was a **report-only** run (non-interactive context →
skill Phase 5 stop-at-Phase-4); nothing in this repo was edited beyond creating this file.

### Phase 1 — deterministic sweep (`scripts/sweep.py`)

| Check | Status | Result |
|---|---|---|
| `dead_reference` | ran | 8 candidates → 5 dismissed at adjudication, 2 merged into 1 finding, 1 dismissed |
| `dead_command` | ran | 0 candidates |
| `boilerplate` | ran | 0 candidates |
| `adr_structure` | ran | 0 candidates (no number collisions, no dangling supersede pointers) |
| `glossary_stats` | ran | 1 glossary, 52 terms, 39 `_Avoid_` terms counted |

**Sweep counts are unreliable and were not trusted downstream.** Verified by direct grep: terms the sweep
scored 0 actually score Execution-fit test 13, THREAD.md 32, Batch 52, Step-back 19, Input-gated 9,
Agent-fixable 7, Pickup 6, Conductor 6, Provisional 4, Provenance 4, Curator 4, Thread-worthy 3,
Four-verb 2, Task floor 1, Clean defer 1, Recall 1. The `_Avoid_` term `executor` was tokenised with a
trailing space (`"executor "`) and scored 0 against 9 real occurrences. `dead_command` also missed
`docs/build-plan.md:86`'s `/wave:schedule`, caught by the semantic pass.

### Phase 2–3 — semantic families

| Family | Status | Candidates → Findings |
|---|---|---|
| **F1** dead references | ran | 12 → **3** (m1, m2, m4) |
| **F2** dead commands | ran | 2 → **0** standalone (both folded: ADR 0003 `/wave:*` → M3; build-plan `/wave:schedule` → S2). All 7 README Tests commands, 5 Install commands, and the `--self-test-classify` flag verified present and working |
| **F3** instruction truth | ran — **Tier 1 absent**, run against nearest analogues (THREAD.md, build-plan.md, windows-setup.md, `_shared/` specs) | 9 → **4** (S1, S3, M1, m7) |
| **F4** ADR-vs-code | ran — all 11 ADRs, each decision extracted as a falsifiable assertion and traced to code | 4 → **2** (M3, m6). ADRs 0001, 0002, 0004, 0005, 0006, 0007, 0008, 0009, 0010, 0011 all verified clean against the engine, `reconcile-wave.py`, `merge-wave.sh` and the skills |
| **F5** glossary drift | ran — all 52 terms grepped; 20 zero-count terms individually cleared | 2 → **1** (m5) |
| **F6** README claims | ran — all 9 assertion areas | 3 → **2** (M2, m3) |
| **F7** legacy narration | ran | 6 → **1** (S2) |
| **F8** duplicated truth | ran | 6 → **1** (D1) |
| **F9** CONTEXT.md purity | ran | 4 → **0** — all refuted. The flagged entries (`dispatched:` stamp lifecycle, Accumulated feedback semantics, Step-back round trigger, Rollout filename form) are constitutive definitions in the file's established house style; condemning any would condemn five-plus structurally identical siblings (Provisional, Pause, Escalation, Curator, Cursor) and gut the glossary's referential function |

### Refutation gate outcome

**30 candidates entered Phase 3; 14 survived (47%).** Notable kills:

- The single **Significant** CONTEXT.md candidate (the rollout-filename form) died on layout: the pointer at
  `schedule/SKILL.md:228` and the fuller `-N` ordinal rule at `:230` are one blank line apart in the same
  numbered subsection, so the claimed harm path does not exist — and the proposed fix would have violated
  `CONTEXT.md:4` "Terms only — no implementation".
- Four of five **F8 duplication** candidates died on the same fault: every copy currently agrees, and each
  has a mechanism-level reason to exist (a glossary defining its terms; a skill `description` that must
  self-trigger and cannot dereference `${CLAUDE_PLUGIN_ROOT}`; format-mandated dual manifests; a SKILL.md
  prose body that cannot import a JS constant).
- Two candidates were internally inconsistent with each other (one demanded CONTEXT.md carry a numeric
  threshold, the other condemned it for carrying a number).
- One candidate's causal claim was disproved by git: `THREAD.md:211`'s "schedule is the next trim candidate"
  was asserted to be false on character count, but handoff (797) and orient (746) both reached their current
  lengths the day **before** the note was written — so the author was ranking *trimmability*, which is
  policy, and policy cannot drift.

### Verified clean (stated explicitly, since a check that did not run is not a clean result)

- **`docs/windows-setup.md`** — all 21 lines. The Stop hook claim matches `hooks/hooks.json:8` verbatim;
  `lachyts/thread-skill` matches `plugin.json.repository`; the python3 requirement holds.
- **Manifests** — `plugin.json` and `marketplace.json` both `2.2.0` (they agree, as `README.md:88-89`
  requires); marketplace `name` = `thread` and `plugins[0].name` = `thread`, so `thread@thread` resolves;
  `plugin.json` correctly has no `hooks` key (removed in `19f7fef`; auto-discovery of `hooks/hooks.json` is
  the intended wiring).
- **"Thirteen skills"** — `ls -1 skills/*/SKILL.md | wc -l` → 13, matching both manifest descriptions and
  README's two tables.
- **Merge policy** — `gh pr merge` appears only inside `merge-wave.sh:285`; `--admin` appears only in three
  prohibition statements, zero uses; no force-push anywhere; `merge-wave.sh:204,235` gate on
  `gh pr checks --required`.
- **Engine architecture claims** — three layers (`converge()` at `wave-execute.workflow.js:963-971`),
  parallel-within-wave (`:985-991`), worktree path (`:638`), `reconcile-wave.py` owning all frontmatter
  transitions (10 subcommands at `:928-986`).
- **ADR 0006/0007/0008 mechanics** — all five escalation triggers present and each guarded by
  `st.tier === 'opus'`; `escalate()` one-way via `if (!st.escalatedAt)`; the `EFFORT` matrix matches ADR 0007
  value-for-value; `planLoop` fails closed to `plan-blocked` on a missing `### Gated inputs` section and
  returns `gate-pending` on any unapproved gate **independent of config**, with `reconcile-wave.py:111-114`
  deliberately excluding `gate-pending` from `BLOCKED_SECTIONS` so `resolve` cannot flip an unsigned gate.
- **MIT attribution** — retained at `skills/handoff/SKILL.md:5-7` as `README.md:124-125` claims.
- **Lineage facts** — `wave--v1.4.1` is the highest wave tag; `047c786` merged wave history; `111d475` is
  "thread 2.0.0 — one plugin, thirteen skills"; `lachyts/wave-skill` is genuinely archived
  (`gh repo view --json isArchived` → `true`); the "2026-06-02 giflab rollout — 8 waves, 14 PRs" figure
  matches `docs/wave-THREAD-archive.md:505-507`.
