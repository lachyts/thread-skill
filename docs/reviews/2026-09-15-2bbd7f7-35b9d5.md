---
repo: /Users/lachlants/repos/tools/thread-skill
head: 2bbd7f7
diff_digest: 35b9d5
harness: claude
engine: code-review
mode: code
effort: high
reviewed: 2026-09-15
status: pending
---

# Clean-room re-review — process-candidate altitude routing

Scope: `git diff HEAD` in this repo — 10 modified files plus the untracked
`docs/adr/0015-process-candidates-route-by-altitude.md`.

| # | file:line | summary | failure scenario | verdict | category |
|---|---|---|---|---|---|
| 1 | `skills/stash/SKILL.md:18` (identically `skills/defer/SKILL.md:18`) | Key the project-ledger gate to close's own project resolution, not `task-writer.md` § 1 | Stash from `~/Projects/Animately/positioning/` with a project-specific observation: § 1 resolves a *vault project note* (and always falls back to a `[[Vault]]`/`[[Personal admin]]` umbrella), never a filesystem directory, so the gate is either unsatisfiable or requires the guess the same sentence forbids — and the project-specific row lands in the Animately seat or estate ledger, the over-routing the method skill says nothing corrects. | HIGH | not emitted |
| 2 | `skills/stash/SKILL.md:18`, `skills/defer/SKILL.md:18` (same hole in close's Commit hygiene template) | Add a `git add` before the pathspec commit, or the first-time ledger commit silently fails | The scan creates `<workspace>/knowledge/METHOD.md` from `METHOD-template.md` and then runs `git -C ~/repos/workspaces commit -m "<msg>" -- <path>`; on an untracked file git errors `pathspec … did not match any file(s) known to git` and commits nothing, and because the scan is contractually silent the new ledger stays uncommitted until the daily sweep absorbs it. (Engine verified the git behaviour in a scratch repo.) | MEDIUM | not emitted |
| 3 | `docs/windows-setup.md:17` | Don't claim seat/estate routing works on Windows while the routing test and `METHOD-template.md` live in the Mac-only standalone-skills tree | A Windows close with the `lachyts/claude-workspaces` clone present and a seat-shaped observation: `~/.agents/skills/method/SKILL.md` (the sole citation from `close:38`) and `METHOD-template.md` are absent per line 3's "standalone skills stay Mac-only", so the agent can neither read the routing test nor create a missing seat ledger — while the same file forbids forking skill bodies and forbids substituting a local ledger. The replaced sentence cited that template absence as the reason capture NOOPs; the new text deletes the reason without shipping the file. | MEDIUM | not emitted |
| 4 | `skills/close/SKILL.md:54` (also `CONTEXT.md:92`, `docs/windows-setup.md:17`) | Root the estate ledger path instead of the relative `_shared/knowledge/METHOD.md` | ADR 0015 newly routes tool repos and any non-project CWD outside `~/repos/workspaces/` to estate, so a close from `~/repos/tools/<x>` reads the unrooted path relative to CWD — actively plausible because this repo has its own `skills/_shared/` — creating a stray `<tool-repo>/_shared/knowledge/METHOD.md` and losing the estate row. The method skill and ADR 0015 both root it. | MEDIUM | not emitted |
| 5 | `skills/close/SKILL.md:38` | Drop the restated routing rungs that contradict the same sentence's "never restated" claim | A later edit to `~/.agents/skills/method/SKILL.md` changes the no-workspace / tool-repo rung; close's restatement ("seat-shaped rows from a tool repo … go to the estate ledger") and stash/defer's restated project-step condition silently keep the old wording, despite each declaring that no second definition of the test lives there — exactly the drift ADR 0012/0015's lockstep clause exists to stop. | LOW | not emitted |
| 6 | `skills/close/SKILL.md:128` vs `docs/adr/0015-process-candidates-route-by-altitude.md:87` vs `skills/stash/SKILL.md:18` | Reconcile the three different accounts of when the ledger gets committed | close defers the seat/estate commit to sub-step 6's workspaces auto-commit, stash/defer commit the ledger per-file immediately with an explicit command, and ADR 0015's Consequences says the per-file auto-commit names the resolved path; a reader following the close route also loses ADR 0012's "versioned immediately rather than waiting on the daily sweep" rationale that motivated the per-file commit. | LOW | not emitted |

## Checked and clean (engine's own list)

Version bump 2.3.4 → 2.3.5 applied in both manifests; the `K<nn>` id form and
`path § heading` evidence form match the method skill's contract; `[correction]`
semantics in `CONTEXT.md` match; ADR 0015's claim that `knowledge/processes.md`
"no longer exists anywhere" is true; amendment notes present on both ADR 0012 and
0014; README's ADR list complete and correctly points at workspaces ADR 0003 as
canonical; close's "`~/Projects` monorepo" in step 7.3 accurate given
identification branch 1; the knowledge-vs-process two-bar split in the
Destinations table matches ADR 0015 and the method skill.

## Run notes

- The engine emitted **severities** (HIGH/MEDIUM/LOW), not CONFIRMED/PLAUSIBLE
  verdicts, and no category field — its prompt asked for "a severity". The
  verdict/category columns above record that faithfully rather than inventing values.
- The `effort:` value in the front matter is pre-filled as dispatched (`high`).
  The engine actually received its **single-pass** prompt variant
  ("minimal prompt → single careful diff pass → ≤15 findings"); see the closing
  notes in the review report.
- No multi-agent fan-out ran. The engine forked to a background agent
  (`ReportFindings` absent in the subagent) and completed as one inline pass; it
  made no `Agent` calls and its prompt contained no fan-out instruction.
