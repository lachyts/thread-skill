---
repo: /Users/lachlants/repos/tools/thread-skill/.claude/worktrees/enabler
head: 1c87bd4
diff_digest: d864e7
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-23
status: pending
---

Clean-room `code-review xhigh` of the uncommitted 2.5.2 enabler diff (engine `defaultBranch`,
merge-wave base, the execute § 4 resolver, the test harness, README § Tests, the audit doc). Cell
`o48-xhigh-v1`: inline angles, no verify phase, so there are no verdict/category columns. 15 findings is
the cap, so treat the list as possibly truncated.

| # | file:line | summary | failure scenario |
|---|---|---|---|
| 1 | skills/execute/SKILL.md:176 | Resolver trusts the locally cached `refs/remotes/origin/HEAD` first and only consults `ls-remote --symref` when it is unset, so a stale or dangling local symref wins over the remote. | Default renamed master→main on GitHub, clone runs `git fetch --prune`; origin/HEAD still points at the deleted origin/master (tested: resolver prints `master`, ls-remote says main) → every `git worktree add … origin/master` fails; if origin/master was left frozen, worktrees are cut from stale code while `gh pr create` targets main. |
| 2 | skills/execute/scripts/merge-wave.sh:427 | BASE is latched from the first OPEN PR's baseRefName and never checked against the other PRs or `args.defaultBranch`; each PR merges into its own base and the sentinel still reads `ok`. | One PR targets a hand-kept `main` copy in a master repo: it is squash-merged there, `ok` is written, the cursor advances, wave K+1 branches from origin/<defaultBranch> without that work — the #30/#31 squash-drop exposure; later messages name the wrong base. |
| 3 | skills/execute/scripts/merge-wave.sh:102 | New behaviour: a master-default repo whose root is on master is now fast-forwarded after every wave (previously skipped); the only safeguard, parking the root on a hold branch, is documented only in a code comment. | A thread-skill self-rollout starts with the root on master; after wave 1 the root fast-forwards and the plugin's live skill text changes mid-rollout, which never happened before this diff. |
| 4 | docs/audits/2026-09-23-thread-audit.md:69 | Docs claim "Fixed in 2.5.2" but both manifests still say 2.5.1, and a same-version change never refreshes the version-keyed cache the engine and merge-wave.sh run from. | Landed unbumped: SKILL.md (live from the tree) tells leads to pass `defaultBranch=master`, the cached 2.5.1 engine ignores it and branches from origin/main; `make release-check` passes because the version still matches the stale cache. |
| 5 | tests/run.sh:23 | The "run wrote nothing" check compares `git status --porcelain --ignored` for skills/hooks/tests only; blind to files inside an untracked dir, to content changes in already-dirty files, and to everything outside those dirs. | Tested: touching tests/lib/zz-probe.tmp and appending to the already-modified merge-wave.sh both leave the porcelain unchanged, so a test writing bytecode/state or rewriting a dirty file passes; docs/, root, .claude/ never checked. |
| 6 | Makefile:16 | release-check captures only diff's stdout: a cache version missing skills/ or hooks/ puts the error on stderr, `d` stays empty, and it reports a match. Also hardcodes $(HOME)/.claude (ignores CLAUDE_CONFIG_DIR) and reads marketplace plugins[0] rather than matching by name. | Partial install leaves $CACHE/2.5.2 without hooks/ → "cache matches" while the Stop hook is missing; under a profile whose plugins/ is not symlinked into ~/.claude it checks the wrong cache. |
| 7 | tests/run.sh:18 | HOME is replaced for the whole run, including interpreter resolution, so asdf/mise shims (which read the global version under $HOME) break. | On a machine with node/python3 from asdf/mise, every node/python step fails "No version is set for command node" — `make test`, the self-rollout verifier, goes red for a reason unrelated to the code. |
| 8 | skills/execute/SKILL.md:220 | Other base-branch references still say origin/main: §4.5 step 4 ("worktrees branch from the freshly-merged origin/main"), line 51 ("merges each wave's approved PRs to main"), line 342, status/SKILL.md:68 ("re-dispatch will branch fresh from origin/main"), the engine's baseline-manifest text ("fail on a clean main"). | A lead/repair session on a master repo reasons about or checks origin/main per §4.5 or /thread:status, contradicting the defaultBranch contract and misdirecting a stale-base or smart-halt diagnosis. |
| 9 | skills/execute/wave-execute.workflow.js:719 | BRANCH_NAME blocks shell injection but admits git-invalid names (`foo.lock`, `foo/`, `/foo`, `a//b`, `.hidden`, `x.`, `a/.b`), so the fail-before-dispatch guard passes values that can never work. | `defaultBranch='release/'` validates, the run starts, every implementer's `git worktree add … origin/release/` fails as an invalid reference; each task burns an agent pass and blocks instead of the run failing once. |
| 10 | skills/execute/SKILL.md:175 | The snippet assigns `R=<repoPath>` unquoted (and the test's sed only matches that form), breaking for repo paths with spaces. | repoPath '/Users/x/My Repo' → `R=/Users/x/My Repo` runs `Repo` as a command; `git -C "$R"` points at the wrong directory; the resolver stops or resolves another repo's branch. |
| 11 | skills/execute/SKILL.md:140 | The canonical args example (a giflab, main-default rollout) hardcodes `"defaultBranch": "master"` although the rule is to omit it for main repos. | A lead copying the example for a main-only repo keeps master; every fresh worktree `git worktree add … origin/master` fails; every task blocks at setup. |
| 12 | tests/default-branch.test.sh:51 | "setup lands in the task worktree" compares $top with `$(cd "$wt" && pwd -P)`; if setup fails and no worktree exists both are empty and the check passes vacuously. | A regression makes `git worktree add` fail: `ok "" ""` prints ok; only the next assertion catches it, pointing at the wrong step. |
| 13 | tests/lib/check-workflow-parse.sh:8 | The script cd's to the repo root before reading its file arguments, so relative paths given from elsewhere resolve against the wrong base. | Tested: `cd skills/execute && bash ../../tests/lib/check-workflow-parse.sh wave-execute.workflow.js` → "No such file", "integer expression expected", FAIL on a valid file. |
| 14 | skills/execute/scripts/merge-wave.sh:204 | Altitude: three independent sources of "the base" — the skill resolver (worktree base), `gh pr create`'s implicit default (PR base), merge-wave's per-PR baseRefName / `gh repo view` fallback — with nothing tying them together. | Any disagreement (stale symref, mixed bases) lets worktrees, PRs and merges land on different branches. Deeper fix: resolve once from the source gh uses, thread it into the worktree base and an explicit `--base` in PR creation (rendered only when not main), pass it to merge-wave and check every PR's baseRefName against it. |
| 15 | tests/lib/engine.mjs:12 | loadEngine duplicates prompt-invariants.test.mjs's vm loader nearly line for line; the g()/ok/sb_ok/st helpers are also duplicated between merge-wave's self-tests and default-branch.test.sh. | Loaders must now change together; if the orchestration marker or meta shape changes and only one is updated, a suite breaks or silently loads a different region. |
