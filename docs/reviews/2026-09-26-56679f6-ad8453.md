---
repo: /Users/lachlants/repos/tools/thread-skill
head: 56679f6
diff_digest: ad8453
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-26
status: pending
---

Target: `574d1a0...ci/make-test` (PR #21 plus this session's ADR 0024/0025, CONTEXT.md and README commits).
The cell is `o48-xhigh-v1`: inline angles, no verify phase, so findings carry no verdict or category.
The engine returned the full 15-finding cap, so treat the list as possibly truncated.

| # | file:line | summary | failure scenario |
|---|---|---|---|
| 1 | docs/adr/0024-the-operators-top-tier-sets-every-rollouts-ceiling.md:25 | The ADR says 0016's terminality and effort rules apply "unchanged", but 0016's tierCapped semantics assume quota comes back, and an operator ceiling never lifts, so every real wall is misread as a temporary cap | Top tier is opus. An opus task hits a real wall, gets marked tierCapped and stamped `tier_capped:`. execute/SKILL.md:294 says to re-dispatch "once quota returns, never read it as a wall", which never happens, so ADR 0006 triage is switched off for every rollout. CONTEXT.md:259 repeats the claim. |
| 2 | docs/adr/0025-master-moves-only-by-green-pr-owner-included.md:17 | The ADR says release and close-out commits "ride a PR", but `/thread:close` 7.1 and `/thread:handoff` auto-commit to the checked-out branch, and with enforce_admins those commits are unpushable | close or handoff on master (like 8f096b6 and 574d1a0) leaves a local master commit that the push rejects. Local master diverges, merge-wave's `merge --ff-only origin/master` stops refreshing the live checkout, and repo-state.sh never flags the stranded commits. |
| 3 | docs/adr/0025-master-moves-only-by-green-pr-owner-included.md:9 | The ADR calls master-only protection an "independent second layer" against the leak, but it covers `master` alone with block_creations false, so the stray-`main` half of the leak is still open | A leaked GIT_DIR test run with the fixtures' `init.defaultBranch=main` does `git push origin main`, which creates a branch on the public repo. Fixing it needs a ruleset blocking creation, or protection on `*`. |
| 4 | CONTEXT.md:248 | The glossary and ADR describe the top-tier ceiling as live, but nothing reads `~/.config/thread/top-tier` and the skills say the opposite. ADR 0024 is marked "accepted", not "accepted, implementation pending" | An agent expects schedule to stamp max_tier, while schedule/SKILL.md:257 ("Never set it as a default…") and execute/SKILL.md:83 say to leave it unset. The implementation is tracked only in vault task p7-1. |
| 5 | docs/adr/0024-…ceiling.md:20 | "execute refuses any tier above it" contradicts "0016 applies unchanged": 0016 lowers an over-cap tier to the cap, it doesn't refuse | Read literally, a resumed rollout with tasks stamped `model: fable` by an earlier escalation is rejected rather than lowered to opus, which blocks repair and resume of any previously escalated rollout. |
| 6 | .github/workflows/test.yml:15 | Required checks are bound to the exact job names, with no warning against renaming them and no recorded restore procedure | A PR renames the job or pins macos-15. The old required contexts never report, so every PR hangs, and under enforce_admins nobody can merge the fix without editing protection by hand. "Lift … then restore" gives no command or payload. |
| 7 | README.md:117 | The README says tag with `claude plugin tag`, then land the bump by PR. The tag goes on local HEAD, and a squash or rebase merge produces a different commit, so the tag isn't in master's history | Bump, `claude plugin tag --push`, open the PR, squash-merge. `thread--vX.Y.Z` points at an orphaned branch commit. The README should say to tag master's merged commit after the merge. |
| 8 | docs/adr/0024-…ceiling.md:19 | "Schedule stamps each rollout's ceiling from it" copies the value into every rollout note, contradicting "changed in one place", with no rule for whether the stamp or the live file wins | Switching the file to fable leaves already-scheduled or paused rollouts at `max_tier: opus`. If the file is lowered after scheduling instead, the result depends on the undefined "refuse" check. |
| 9 | CONTEXT.md:253 | The Top tier, Ceiling and Capture edits land on master, although the rollout brief says to decline CONTEXT.md edits because the redesign rewrites that file, and the redesign's ADR 0021 rejects legacy tier knobs | Integrating protocol 4 (p6-1) will conflict here: either the entries are silently dropped, or ADR 0024's max_tier becomes a knob protocol 4 refuses. |
| 10 | .github/workflows/test.yml:22 | actions/checkout@v4 and setup-node@v4 run on the deprecated Node 20 action runtime, and PR #21's run already warns about it | When GitHub stops forcing Node 24 or removes the runtime, both required checks break and block every merge under enforce_admins. Bump to @v5. |
| 11 | CONTEXT.md:248 | The glossary's "Top tier" (the operator's highest permitted tier, e.g. opus) collides with the engine's `TOP_TIER = 'fable'` (wave-execute.workflow.js:790), and the _Avoid_ list doesn't flag it | p7-1 wires the glossary value into `TOP_TIER`. Escalation becomes an opus-to-opus no-op, effortTier collapses, and capped tasks lose ADR 0016's effort uplift. |
| 12 | tests/repair-stranded-merged.test.sh:117 | gawk and mawk turn the `\\.` in `awk -v s='^### 2\\.'` into `.`, so on Linux the region patterns are wildcards | The Ubuntu job prints 7 escape-sequence warnings, and `'^### 2.'` matches `### 20`, so the assertions are looser on Linux than on macOS. Use ENVIRON or `[.]`. |
| 13 | .github/workflows/test.yml:14 | There's no `timeout-minutes`, so a hung suite holds both required checks for the 360-minute default | A background suite or merge-wave push blocks on a credential prompt, and the required check stays pending for 6 hours, holding merge-wave and every PR. |
| 14 | THREAD.md:431 | THREAD.md still lists "no CI and no branch protection" as an open question | The next session re-plans p5-1, or assumes direct pushes to master still work. |
| 15 | docs/adr/0016-a-tier-ceiling-caps-escalation-when-quota-is-gone.md:4 | ADR 0016 has no "Amended by ADR 0024" note (0011, 0012 and 0014 have theirs), and the README's ADR index stops at 0017 | A reader starting from 0016 or the skills never finds 0024. 0024 and 0025 can only be found by listing the directory. |

Beyond the cap (engine-reported): `fetch-depth: 0` isn't needed and there's no `concurrency` group; no
`persist-credentials: false`; 7a5fbbd bundles unrelated ADRs with the Capture entry (not atomic); an existing
bug behind the Capture entry, where the task-writer §2 regex misses block-style YAML tags (27 vault tasks).
