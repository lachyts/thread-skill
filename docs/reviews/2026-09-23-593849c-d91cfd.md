---
repo: /Users/lachlants/repos/tools/thread-skill/.claude/worktrees/enabler
head: 593849c
diff_digest: d91cfd
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-23
status: pending
---

Round 2 — clean-room `code-review xhigh` of `origin/master...593849c` (the committed 2.5.2 enabler
after round 1's fixes). 12 findings (under the cap of 15). Cell `o48-xhigh-v1`: no verdict/category.

| # | file:line | summary | failure scenario |
|---|---|---|---|
| 1 | skills/execute/scripts/merge-wave.sh:425 | The one-base-per-wave guard fails open: a failed `gh pr view … state` read makes the pre-pass `continue` past that PR instead of halting, so its base is never checked. | PRs 11 (master) and 12 (main); #12's state read hits a transient gh error → skipped; the merge loop reads it OPEN and merges it into main — the mixed wave the guard exists to stop. The merge loop treats a failed read as fatal; the guard does not; no test covers it. |
| 2 | skills/execute/scripts/merge-wave.sh:417 | The guard checks PRs only against each other, never against the base the engine cut worktrees from; merge-wave takes no expected-base argument, so the comment's promise is unenforced. | defaultBranch='develop' while GitHub's default is main: an implementer skips `--base develop`, its PR targets main; in a single-PR (or all-skipped) wave BASE='main' and it auto-merges into production; the next wave branches from origin/develop without it. The all-merged `gh repo view` fallback and the test accepting a MERGED PR into another base never compare either. |
| 3 | skills/execute/SKILL.md:183 | defaultBranch is not durable (no rollout-note field); every per-wave call, cold resume, heartbeat re-entry or repair re-derives it from the remote, losing a manual override; /thread:status cites `origin/<defaultBranch>` with no source. | Wave 1 runs with a manual defaultBranch='develop'; a fresh session resumes at wave 2, re-resolves 'main' and omits it → wave 2 branches from origin/main without wave 1's merges into develop, PRs target main. Likewise a later-wave session that skips the resolver on a repo with a stale hand-kept origin/main. |
| 4 | skills/execute/wave-execute.workflow.js:735 | prBase treats defaultBranch 'main' as unset and adds no `--base`, so PRs go to gh's implicit repo default while worktrees are cut from origin/main. | GitHub default is `develop`, mainline `main`; the lead passes 'main' per SKILL.md; worktrees from origin/main, PRs to develop; merge-wave sees them agree on develop and merges there; next wave branches from origin/main without them — silent squash-drop. |
| 5 | tests/run.sh:66 | tree_unchanged prunes only `$root/.git`, so from the main checkout it walks every nested worktree under `.claude/worktrees/` and the merge-wave sentinel — other sessions' activity reads as a false failure. | `make test` in the root checkout while a self-rollout's agent worktrees are being edited, or merge-wave writes `.claude/merge-wave.status`: any file touched there in the run window fails the suite with nothing wrong. Fix: prune `$root/.claude`. |
| 6 | skills/execute/scripts/merge-wave.sh:422 | The pre-pass promises "Nothing was merged" only for mixed bases; it skips CLOSED/non-OPEN PRs, which then halt the merge loop after earlier PRs have already merged. | `merge-wave.sh repo 31 32 33` with #33 closed: the pre-pass skips #33, #31 and #32 merge, then the loop exits 1 on "PR #33 is 'CLOSED'" — a partial halt, though the pre-pass already read every state. |
| 7 | skills/execute/diagnostics/edit-noop-repro.workflow.js:72 | The diagnostic claims to mirror the engine's worktree setup "verbatim" but hardcodes `origin/main` (drifted from defaultBranch) and is not idempotent across re-runs. | (a) A scratch repo with init.defaultBranch=master: `worktree add … origin/main` fails, every probe reports a setup failure. (b) A second run reuses repro-i whose anchors were already replaced; both Edits fail on missing anchors — a false positive for the no-op bug. |
| 8 | skills/execute/SKILL.md:220 | Operative `main` assumptions remain: §4.5 step 4 "worktrees branch from the freshly-merged origin/main" (:220), halts leaving work "on `main`" (:357), serial merges "into a strict-protected main" (:435), and the engine's baselineManifest prompt "fail on a clean `main`" (wave-execute.workflow.js:259). | On a master/develop rollout the lead following §4.5 step 4, or an agent reading the baseline manifest, is pointed at a `main` that does not exist or is wrong — contradicting §4 and the engine's own base. |
| 9 | skills/execute/SKILL.md:140 | The args example carries a literal `"defaultBranch": "<branch>"` while its comment says the example omits it; a lead copying it passes the placeholder. | Copied verbatim for a main repo: the engine's top-level defaultBranch(a) rejects "<branch>" and the run throws before dispatch — loud, but self-inflicted by a contradictory example. |
| 10 | skills/execute/scripts/merge-wave.sh:437 | The two loops duplicate PR-number parsing/validation, and each PR costs three sequential `gh pr view` calls (state, baseRefName, state again). | ~3N gh calls per wave before merging, and parse/validate in two places that can drift; one pre-pass reading `--json state,baseRefName` and storing the parsed numbers/states would halve the calls. |
| 11 | tests/lib/engine.mjs:12 | loadEngine re-implements prompt-invariants.test.mjs's vm loader; prompt-invariants was not migrated. | The marker-slice and meta rewrite live in two copies; an engine header change breaks or drifts one while the other passes. |
| 12 | tests/contracts/manifest.test.mjs:42 | Half of "every skill directory has a SKILL.md whose name is the directory" cannot fail: skillDirs is pre-filtered to dirs that already contain a SKILL.md. | A skill dir loses its SKILL.md: silently dropped; passes as long as the count stays ≥13 (adding one skill and losing another does). An explicit non-skill allowlist (`_shared`) would make it real. |
