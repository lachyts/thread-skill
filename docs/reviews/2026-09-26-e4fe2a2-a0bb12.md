---
repo: /Users/lachlants/repos/tools/thread-skill
head: e4fe2a2
diff_digest: a0bb12
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-26
status: consumed
---

Target: `9425f41...e4fe2a2` (round 2: the fixes for review 026dd42/20344a). Cell `o48-xhigh-v1`, ungraded.

| # | file:line | summary | failure scenario |
|---|---|---|---|
| 1 | skills/close/SKILL.md:21 | The fix only holds when close is Skill-loaded; `/thread:open save` and `/thread:next` read close as a file, so its `${CLAUDE_PLUGIN_ROOT}` stays literal | Bash tool has it unset → `bash /skills/open/scripts/repo-thread-lookup.sh` rc 127 → the new rule skips the thread update; handoff-scan, repo-state, repo-track wrappers exit 2 the same way |
| 2 | skills/close/SKILL.md:21 | On a failed lookup close skips the thread update but step 9 still prints "Safe to end this session" | Decisions, log line and resume pointer lost when Lachy closes the pane |
| 3 | skills/close/SKILL.md:21 | A failed lookup skips the thread update but not the handoff lifecycle | Step 7.2 deletes a consumed doc the un-rewritten Resume pointer still names; an empty slug drops the `_shared` thread filter |
| 4 | skills/close/SKILL.md:21 | `<slug>` unquoted in `slug=<slug> bash "..."` | A basename slug like `overlay-carousel (codex)` → zsh parse error rc 1 → read as a failed lookup |
| 5 | skills/close/SKILL.md:21 | The explanatory prose itself contains `${CLAUDE_PLUGIN_ROOT}`, which is substituted, so it contradicts itself (also open/SKILL.md:86) | Rendered text says "keeps `/Users/…/thread-skill` literal" — a false reason |
| 6 | skills/_shared/task-writer.md:63 | Altitude: any runnable `${CLAUDE_PLUGIN_ROOT}` command in a read-not-rendered file stays literal; the fix special-cases one caller | stash/defer via task-writer.md run `resolve-day.py` through an empty root; handoff-home.sh via next the same |
| 7 | .gitattributes:2 | `eol=lf` applies only on re-checkout; extracted SKILL.md snippets are not covered | An existing autocrlf Windows clone keeps CRLF scripts until renormalised |
| 8 | skills/execute/SKILL.md:183 | The usage-error branch can't fire from the wrapper; an empty repoPath passes and `git -C ""` uses the CWD's repo | `R=""` → prints the CWD repo's default branch, rc 0 |
| 9 | skills/close/SKILL.md:187 | Step 7.1 re-runs the full lookup to classify the resolved path; the failure rule is undefined there | A rung-1 project thread's update skipped or left uncommitted when the second lookup fails |
| 10 | skills/open/SKILL.md:54 | The failure rule lives only in § Repo-thread lookup Callers; `<slug>` step 3 and the pickup's step 3 still read empty as not-found | `/thread:open foo` with exit 2 offers to create a duplicate thread |
| 11 | tests/repo-threads.test.sh:177 | The "unset" case exports an empty variable, skips the stderr check, and no test runs close's actual command | A `${CLAUDE_PLUGIN_ROOT-…}` fallback or quoting change still passes |
| 12 | skills/open/SKILL.md:86 | Two call forms fail differently: open's exit 2 + `repo-thread-lookup:` vs close's bare `bash` rc 127 | Consumers expecting one failure shape misread the other |
| 13 | skills/close/SKILL.md:169 | The `command grep` rationale claims a `BASH_ENV` guard while awk, sort, head, basename and `command` itself stay unguarded | A `BASH_ENV` defining `awk()` or `command()` corrupts output while the text says the risk is handled |

## Disposition (2026-09-27, consumed): ledger STOP at round 2 (10 of 12 classified are regressions of 7cee1a5)

The round-1 fix is reverted (`d809c95`). The root fix (`01c1e91`) keeps open's lookup inline, rewritten with
no positional parameter and no plugin-root path, so close cites it exactly as on master.

- **2, 3, 4, 5, 9, 10, 11, 12:** gone with the revert. They were holes in the reverted rule and wording.
- **8:** fixed in `cb8ccb0`. An empty `<repoPath>` is now a usage error (exit 2), and there is a test for it.
- **13:** fixed in `cb8ccb0`. The note no longer claims a `BASH_ENV` guard.
- **1, 6, deferred to a follow-up task:** any `${CLAUDE_PLUGIN_ROOT}` command in a file that is read
  rather than rendered stays literal. This predates p5-2: close's handoff-home.sh and repo-state.sh
  calls, task-writer.md's resolve-day.py call. It needs one rule stated once, which is its own task.
- **7, recorded:** `eol=lf` applies from the next checkout. An existing autocrlf clone renormalises
  once, with `git rm --cached -r . && git reset --hard`. Extracted snippets run only in `make test`,
  on macOS and Linux.
