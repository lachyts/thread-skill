---
repo: /Users/lachlants/repos/tools/thread-skill
head: 036f69e
diff_digest: 0c39da
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-17
status: consumed
---

# Review — wave engine tier ceiling (`args.maxTier`)

Target: the uncommitted working-tree diff at `036f69e`, one file, `skills/execute/wave-execute.workflow.js` (+22 -10).

| # | file:line | summary | failure scenario | verdict | category |
|---|---|---|---|---|---|
| 1 | `skills/execute/wave-execute.workflow.js:331` | Render the Ralph loop when opus is the terminal tier, not the one-shot verify block. | A task note stamped `model: fable` with `max_iterations: 8` dispatched under `maxTier:'opus'` renders the one-shot verify block on both passes, so it gets two single verifier runs and zero fix iterations before exiting blocked, where it previously ran the full 8-iteration Ralph loop. | not emitted | not emitted |
| 2 | `skills/execute/wave-execute.workflow.js:726` | Record the suppressed escalation in the result contract, not only in an ephemeral log line. | A hard task capped at opus returns `escalated=false` with no tier-capped field, so reconcile never stamps `model: fable` and /thread:repair triages the task as a genuine wall that is never re-dispatched once fable quota returns. | not emitted | not emitted |
| 3 | `skills/execute/wave-execute.workflow.js:1050` | Pass `a` to the missed `taskModel(t)` call site so the fallback tier is clamped. | When converge() yields a falsy result, `taskModel(t)` is called with one argument, so `tierCap(undefined)` returns fable and a task stamped `model: fable` in a capped run is reported as having run on a model the account had no quota for. | not emitted | not emitted |
| 4 | `skills/execute/wave-execute.workflow.js:722` | Make clampTier a rank comparison; it currently ignores its tier argument and can raise a tier. | A `judgeModel` pin below opus (or any unrecognised free-form value) is silently upgraded to opus by the cap branch, spending more rather than less, and the line-767 unknown-tier guard becomes unreachable whenever the cap is on. | not emitted | not emitted |
| 5 | `skills/execute/wave-execute.workflow.js:723` | Clamp only the model dimension; the cap collapses the whole (model, effort) bundle. | Pinning `st.tier` to opus also pins the EFFORT row, so every implementer stays at medium and every master review at high even though effort consumes no fable quota under the feature's own stated operating condition. | not emitted | not emitted |
| 6 | `skills/execute/wave-execute.workflow.js:326` | Stop promising a stronger-tier takeover in the retry prompts when the cap makes one impossible. | Under `maxTier:'opus'` the one-shot block still tells the first-pass agent a stronger model will pick up its worktree and the escalation context still calls the second pass a stronger-tier takeover, so the agent deliberately stops early and withholds the fix it is in fact the last party able to make. | not emitted | not emitted |
| 7 | `skills/execute/wave-execute.workflow.js:721` | Normalise maxTier and log the resolved regime; it currently fails open on any malformed value. | `maxTier: 'Opus'`, `'opus '` or a mis-cased key leaves the cap disengaged, so the run escalates and burns the exhausted fable quota with nothing in the log saying which regime was chosen. | not emitted | not emitted |
| 8 | `skills/execute/wave-execute.workflow.js:928` | Finish the label fix; two sibling labels still hard-code `@fable`. | Lines 816 and 928 keep `plan:<slug>@fable` and `implement:<slug>@fable`, so a capped run's /workflows view and every log line label opus dispatches as fable, which is the one thing an operator watching a quota-exhausted run needs to read correctly. | not emitted | not emitted |
| 9 | `skills/execute/wave-execute.workflow.js:715` | Plumb maxTier through the skill that owns config resolution; the knob is unreachable as shipped. | `maxTier` appears only inside the engine file, with no row in the SKILL.md args table or frontmatter mapping and no key in the rollout template, so a lead session following the documented dispatch path can never set the cap without hand-editing the Workflow tool call. | not emitted | not emitted |
| 10 | `skills/execute/wave-execute.workflow.js:20` | Add maxTier to the file's own args contract docblock and update the Returns block. | Lines 20-70 document every other optional arg with its absent-is-byte-identical note and lines 73-83 still describe `escalated`/`escalatedAt` without the suppression case, so anyone reading the contract to build args or add the missing plumbing will not know the arg exists. | not emitted | not emitted |
| 11 | `skills/execute/wave-execute.workflow.js:721` | Add test coverage; the four new and changed tier functions are not exported to the harness. | `tierCap`, `clampTier`, `taskModel` and `escalate` are absent from the `__t` export list at tests/prompt-invariants.test.mjs:35, so the suite passes only because it never touches the new code, and a capped-run case would have caught findings 1, 3 and 8 immediately. | not emitted | not emitted |
| 12 | `skills/execute/wave-execute.workflow.js:828` | Give planLoop and reviewLoop explicit terminal returns; both fall off the end returning undefined. | With `max_plan_rounds: 0` or a non-numeric YAML value the while-loop never executes and planLoop returns undefined, so implement() sees no prev and the task is implemented with no plan, no plan judge and no plan-stage gated-inputs parse, failing open on the ADR 0008 gate check. | not emitted | not emitted |
| 13 | `skills/execute/wave-execute.workflow.js:715` | Amend ADR 0006 and CONTEXT.md, and bump the plugin version, for a behavioural change. | ADR 0006 states flatly that opus never runs the Ralph loop and that there is no config switch, CONTEXT.md still defines escalation as one-way and sticky with no ceiling concept, and plugin.json is still 2.3.5, so the next reader resolves the conflict in favour of the ADR and deletes the feature. | not emitted | not emitted |
| 14 | `skills/execute/wave-execute.workflow.js:722` | Resolve the run-constant cap once instead of re-deriving it at every tier decision. | `tierCap(a)` is a pure function of an arg that never changes during a run yet is recomputed by clampTier, taskModel, judgeFor and escalate, each needing `a` threaded to it, which is exactly the threading burden that produced the missed call site at line 1050. | not emitted | not emitted |

**Verdict and category columns.** The engine emitted findings with four fields only — `file`, `line`, `summary`, `failure_scenario`. It gave no `verdict` and no `category`, so both columns are recorded as not emitted rather than filled in by the reporting agent.

**How this review ran.** The `code-review` skill forks itself into a background subagent. That subagent used Bash only across all runs in this session and spawned no Agent subagents, so the multi-agent fan-out did not run and the review was a single inline pass at `xhigh`. The engine read the diff, the surrounding file, `skills/execute/SKILL.md`, the ADRs, and ran `node tests/prompt-invariants.test.mjs`.

**First dispatch reviewed the wrong tree.** A bare `Skill(code-review, "xhigh")` invocation resolved its diff against the session launch directory (`/Users/lachlants/repos/workspaces/ops-workspace`) rather than the Bash working directory, and returned eleven findings about `agent-quotas-poll.py`. Those findings are out of scope for this document and are not recorded here. The run above is the retry, dispatched with an explicit path target.

## Appendix — engine output, verbatim

```json
[
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 331,
    "summary": "Under maxTier:'opus' no agent ever runs the Ralph verification loop \u2014 st.tier is permanently 'opus', so verifyBlock always renders oneShotVerify and task.maxIterations becomes dead code.",
    "failure_scenario": "A task note stamped `model: fable` with `max_iterations: 8` is dispatched with maxTier:'opus'. taskModel clamps the seed to opus, so the implementer prompt (line 423/576) renders oneShotVerify: 'Run the verifier EXACTLY ONCE\u2026 do NOT attempt a fix, do NOT run the verifier again'. It goes red, escalate() is a no-op, and the retry at line 928 re-renders the SAME one-shot block on opus. The task gets two single verifier runs, zero fix iterations, and exits `blocked`. Before the change that same task ran the full 8-iteration Ralph loop from the start. The comment's justification \u2014 'the alternative is not a weaker run but no run at all' \u2014 is false: the capped run is strictly weaker than the fable loop it replaces. A terminal opus tier must render ralphLoop, not oneShotVerify (the one-shot only makes sense as a hand-over protocol with a stronger tier behind it)."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 726,
    "summary": "escalate() returns before setting st.escalated, so a quota-capped block is reported identically to a genuine wall \u2014 ADR 0006's triage invariant is inverted with no persisted trace.",
    "failure_scenario": "A hard task hits the escalation trigger under the cap. The only record is an ephemeral `log()` line in /workflows. The result contract carries escalated=false, escalatedAt='', and no 'tier-capped' field; reconcile-wave.py stamps `model: fable` only `if task.get(\"escalated\")` (line 499), so the task note ends up `status: blocked` with a plain verifier diagnosis. ADR 0006 states the consequence this guarantees \u2014 'a block is a genuine wall or an input-gated decision, never \"the cheap model wasn't enough\", which sharpens /thread:repair's input-gated-vs-agent-fixable triage' \u2014 and that is now false. /thread:repair (and the human reading the note) classifies the task as a wall and never re-dispatches it once fable quota returns, which is exactly the task the cap was supposed to defer."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 1050,
    "summary": "Missed call-site update: `taskModel(t)` in the result-normalisation block still passes one argument, so the fallback tier is never clamped.",
    "failure_scenario": "taskModel's signature became (task, a). At line 1050 `model: norm.model || taskModel(t)` passes no `a`, so tierCap(undefined) returns 'fable' and clampTier passes the tier through unchanged. This path fires whenever converge() yields a falsy result \u2014 pipeline() returning null on a thrown stage, or reviewLoop/planLoop falling off the end (see the maxPlanRounds finding). A task stamped `model: fable` in a maxTier:'opus' run is then reported `model: 'fable'`, contradicting the documented contract at line 77 ('model is the FINAL tier the task ran on') and telling the wave report the run used a model the account had no quota for."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 722,
    "summary": "clampTier ignores its `tier` argument on the cap branch \u2014 it unconditionally returns 'opus', so it is an override that can RAISE a tier, not a ceiling.",
    "failure_scenario": "`tierCap(a) === 'opus' ? 'opus' : tier` never inspects `tier`. Called as judgeFor(a, st) with a judgeModel pin below opus (or any unrecognised value \u2014 the arg is free-form and unvalidated), the cap silently upgrades the judge to opus, spending more rather than less. It also makes the deliberate guard at line 767 (`EFFORT[judgeFor(a, st)] || EFFORT[st.tier]`, commented 'guards a judgeModel value with no matrix row') unreachable whenever the cap is on, since judgeFor can then only return 'opus'. The correct form is a rank comparison: RANK[tier] > RANK[cap] ? cap : tier."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 723,
    "summary": "The cap collapses the whole (model, effort) bundle although only the model is quota-scarce, weakening the run twice for one constraint.",
    "failure_scenario": "implEffort/judgeEffort index EFFORT[st.tier], and st.tier is pinned to 'opus' for the whole capped run. Every implementer stays at `medium` and every master review at `high`, where the escalated tier would have given `high`/`xhigh`. Effort consumes no fable quota, so under the feature's own stated operating condition ('the account's fable quota is exhausted') the run gives up capability it could still pay for. ADR 0007 defines a tier as a (model, effort) bundle; the ceiling should clamp only the model dimension and take the escalated tier's effort row."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 326,
    "summary": "Both retry prompts make false statements to the agent under the cap: they promise a stronger-tier takeover that will never happen.",
    "failure_scenario": "oneShotVerify tells the first-pass agent 'return escalate=true\u2026 A stronger model picks up your worktree and iterates from there', and escalationContext (line 342) tells the second-pass agent 'you are the STRONGER-TIER takeover of this task \u2014 a first-pass attempt at a lower tier did not land it'. Under maxTier:'opus' both passes are the same model at the same effort and no takeover follows. The agent is explicitly instructed to stop early, not attempt a fix, and hand off work it is in fact the last party able to do \u2014 so the cap causes the agent to deliberately withhold effort."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 721,
    "summary": "tierCap fails OPEN on any malformed maxTier value, silently and with no startup log of the resolved regime.",
    "failure_scenario": "Only the exact string 'opus' engages the cap. `maxTier: 'Opus'`, `'opus '`, `'OPUS'`, `'fable'`-typo, or a mis-cased key leaves tierCap at 'fable', escalation proceeds, and the run burns the exhausted fable quota \u2014 precisely the failure the feature exists to prevent \u2014 with nothing in the log saying which regime was chosen. The repo already normalises operator-supplied strings elsewhere (unapprovedGates, line 224, is case/whitespace-insensitive by design); reuse that, and log the resolved cap once alongside the existing `wave-execute: \u2026` startup line (line 1022) so the operator can see it in /workflows."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 928,
    "summary": "Half-applied label fix: the diff changed the read-only retry label to @${st.tier} but left two sibling labels hard-coded to @fable.",
    "failure_scenario": "Line 893 became `investigate:${task.slug}@${st.tier}`, but line 816 (`plan:${task.slug}@fable`) and line 928 (`implement:${task.slug}@fable`) still hard-code the tier. In a capped run the live /workflows view and every log line label opus dispatches as `@fable`, which is the one thing an operator watching a quota-exhausted run needs to read correctly. Note tests/prompt-invariants.test.mjs scenario B asserts `call('implement:proj-eff-b@fable')` verbatim, so the test must be updated in the same change \u2014 which is presumably why this site was skipped."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 715,
    "summary": "maxTier has no plumbing from the skill that owns config resolution, so the knob is unreachable through the documented dispatch path.",
    "failure_scenario": "grep shows `maxTier` appears only inside this engine file. skills/execute/SKILL.md \u00a74 'Build the `args` object the workflow expects' (line 133-146) enumerates every arg the lead session sets \u2014 concurrency, knownBaselineFailures, envBootstrap, progress, and the per-task keys \u2014 and the frontmatter\u2192args table (lines 75-85) has no `max_tier` row; skills/schedule/rollout-template.md has no such key either. A lead session following the skill can never set the cap; it only works if a human hand-edits the Workflow tool call, which is exactly the 'fumble-prone prose choreography' ADR 0006 rejected."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 20,
    "summary": "The new top-level arg is missing from the file's own `args` contract docblock, which documents every other optional arg.",
    "failure_scenario": "Lines 20-70 are the stated interface for this script: rolloutSlug, repoPath, verifier, date, concurrency, knownBaselineFailures, envBootstrap, progress, and every per-task key each get an entry, and every optional one carries its 'Absent/empty \u21d2 byte-identical' note. maxTier (and the pre-existing judgeModel) appear nowhere in it \u2014 only in a comment 650 lines away. The Returns block (lines 73-83) also still describes escalated/escalatedAt without the suppression case. Anyone reading the contract to build args, or to add the missing skill-side plumbing, will not know the arg exists."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 721,
    "summary": "Zero test coverage: the four new/changed tier functions are not in the test harness's export list, so the feature cannot be exercised at all.",
    "failure_scenario": "tests/prompt-invariants.test.mjs is this repo's guard for exactly these invariants (EFFORT matrix, judgeEffort with a judgeModel pin, escalation scenarios A-D, converge end-to-end), and its `__t` export list at line 35 was not extended with tierCap / clampTier / taskModel / escalate. The suite still passes (verified: ALL PASS) only because it never touches the new code. The file header states the per-feature convention every other optional feature satisfies \u2014 an 'absent \u21d2 byte-identical' case plus a set case \u2014 and a capped-run case would have caught findings 1, 3 and 8 immediately."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 828,
    "summary": "planLoop and reviewLoop fall off the end returning undefined when maxPlanRounds/maxReviewRounds is 0 or non-numeric; planLoop's undefined silently SKIPS the plan gate entirely.",
    "failure_scenario": "`while (round <= task.maxPlanRounds)` with max_plan_rounds: 0 (or a non-numeric YAML value \u2192 NaN) never executes and the function returns undefined. implement() then sees `prev === undefined`, so `if (prev && prev.blocked)` is skipped, planExtra is undefined, and the task is implemented with NO plan, NO plan judge, and NO plan-stage gated-inputs parse \u2014 a fail-open on the ADR 0008 gate check, from a config value the skill never validates. The reviewLoop twin (line 956) returns undefined, converge returns undefined, and the task is reported with the generic 'workflow stage threw \u2014 see /workflows'. Both loops need an explicit terminal return; these are pre-existing but sit in functions the diff touches and feed finding 3."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 715,
    "summary": "The change contradicts ADR 0006 and CONTEXT.md without amending either, against the repo's own decision-record and version-bump convention.",
    "failure_scenario": "docs/adr/0006-one-shot-first-pass-iteration-escalates-to-fable.md states flatly: 'Opus never runs the Ralph loop: max_iterations is fable's budget' and 'There is no config switch.' This diff adds a config switch that makes opus terminal. CONTEXT.md \u00a7 Model tiering (line ~219) still defines Escalation as 'the run-time, evidence-driven flip of an opus task to fable\u2026 One-way and sticky (ADR 0006)' with no ceiling concept. The repo's established pattern for a behavioural amendment is a new ADR naming what it amends (0013 'amends', 0015 'amends 0012 + 0014') plus a .claude-plugin/plugin.json bump \u2014 neither happened; the version is still 2.3.5. The next reader resolves the conflict in favour of the ADR and deletes the feature."
  },
  {
    "file": "skills/execute/wave-execute.workflow.js",
    "line": 722,
    "summary": "The run-constant cap is re-derived on every tier decision instead of being resolved once, which is also what made the missed call-site (finding 3) possible.",
    "failure_scenario": "tierCap(a) is a pure function of an arg that never changes during a run, yet clampTier calls it, taskModel calls clampTier, judgeFor calls clampTier on every single judge dispatch, and escalate calls tierCap again on every escalation trigger \u2014 and each needs `a` threaded to it, which is exactly the threading the line-1050 call site missed. Resolve the cap once beside `const a = \u2026` (line 1019) or onto `st` in converge (line 1002), and three functions plus the new fourth parameter on escalate collapse into one value that cannot be forgotten at a call site."
  }
]
```

## Disposition (2026-09-17, lead session event-script-backlog-orient)

Finding 1 was verified in the code before anything else and was correct: `verifyBlock` selected on the
tier NAME, so a capped run rendered the one-shot hand-over with no stronger tier behind it. The wave
that was in flight under the first implementation (`wf_859590bf-a0e`) was **stopped mid-run** on that
basis, not allowed to finish.

| # | outcome |
|---|---|
| 1 | fixed — `verifyBlock` selects on **terminality**; a capped tier renders the full Ralph loop. Pinned by a test asserting the capped block equals the fable block and differs from the one-shot. |
| 2 | fixed — `st.capSuppressed` → `tierCapped` on the result + docblock + SKILL.md report rule saying a `tierCapped` block is not a wall. Escalation is still never stamped, so reconcile cannot write an unusable tier. |
| 3 | fixed — the cap is resolved once (`st.cap`); the `taskModel(t)` normalisation call site now passes it. |
| 4 | fixed — `clampTier` is a rank comparison against `TIER_RANK`; it can only lower a tier, never raise one. Test covers both directions. |
| 5 | fixed — effort is no longer capped: `effortTier()` takes the higher tier's row once an escalation is suppressed (implementer high, master review xhigh). |
| 6 | fixed — `escalationContext(prior, sameTier)` renders a "SECOND PASS … no stronger tier is available" framing under a cap; the takeover framing is unchanged when an escalation really happened. |
| 7 | fixed — `tierCap` normalises case/whitespace and fails **closed** to opus on an unrecognised value, with a log line; the resolved regime is logged once at startup. |
| 8 | fixed — the two remaining `@fable` labels are now `@${st.tier}`. Scenario B still passes because a real escalation leaves the tier at fable. |
| 9 | fixed — `max_tier` added to SKILL.md's frontmatter→args table, the args block and the report rules, so the knob is reachable through the documented dispatch path. |
| 10 | fixed — `maxTier` and the pre-existing `judgeModel` both added to the args docblock; the Returns block documents `tierCapped`. |
| 11 | fixed — the test harness exports the tier functions and 18 assertions cover parsing, clamping, suppression, terminality, effort and prompt framing. Suite: 152 assertions, ALL PASS. |
| 12 | **not fixed — pre-existing, filed.** `planLoop`/`reviewLoop` fall off the end returning undefined when `max_plan_rounds`/`max_review_rounds` is 0 or non-numeric, and an undefined `planLoop` result silently skips the plan gate (a fail-open on the ADR 0008 gated-inputs check). Out of scope for a tier-ceiling change and it wants its own fix plus config validation in the skill. |
| 13 | fixed — ADR 0016 written, naming what it amends in 0006 and why a resource fact is not the config switch 0006 forbade; `CONTEXT.md` § Model tiering gains **Ceiling**; `plugin.json` bumped 2.3.5 → 2.4.0. |
| 14 | fixed — same change as 3: the cap is a run constant resolved once onto `st`, so no call site has to remember to thread it. |
