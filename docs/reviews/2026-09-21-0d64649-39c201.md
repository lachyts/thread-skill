---
repo: /Users/lachlants/repos/tools/thread-skill
head: 0d64649
diff_digest: 39c201
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-21
status: consumed
---

> Transcribed by the main thread from the forked `code-review` engine's findings, relayed
> verbatim by the `fresh-review` wrapper — which held the payload but, correctly under the
> fork clause, wrote no doc (METHOD K26). HEAD moved from `0d64649` to `d17ddb4` mid-review
> because the main thread committed the round-1 doc's disposition; the working-tree diff the
> engine read was unchanged. Round 2 of the durable-handoff-lifecycle + close-suppression
> change; round 1 is `2026-09-21-ed4b2b4-e65597.md`.

## Findings, verbatim

| # | file:line | summary | failure scenario |
|---|---|---|---|
| 1 | skills/handoff/SKILL.md:57 | Per-directory carve-out is hard-coded to the workspaces repo, so in the `~/Projects` monorepo every project's handoff docs collapse into one shared `~/Projects/docs/handoffs/` | `git -C ~/Projects/Animately/giflab rev-parse --show-toplevel` → `/Users/lachlants/Projects`; `~/Projects/.git` exists, so the non-repo fallback never fires and round-1 finding 3's rationale ("not a git repo") is factually wrong. Every project writes to the repo root, contradicting the one real handoff doc on disk at `~/Projects/Life/26-09-14_unicorse-costume/docs/handoffs/`, and close's ownership scan in one project sees another's docs. |
| 2 | skills/close/SKILL.md:14 | Head-of-file "Auto-execute, no asking" contract still names exactly two commit targets while step 7.2 commits to an arbitrary third repo | Line 14 promises commits in `~/repos/workspaces/` and the vault; step 7.2 runs `git -C "$top" commit` / `git rm -f` in whatever repo the session is in. Guardrails is silent too. |
| 3 | skills/close/SKILL.md:61 | Destinations table's "exactly one destination" invariant still broken — the handoff-doc row claims scan category 1, which the THREAD.md row also claims | Table row routes "what is done" to the pending doc; rule 2 says categories 1, 2, 4, 5 land in THREAD.md and only category 3 goes to the doc. |
| 4 | skills/close/SKILL.md:70 | Ownership test keys on the active THREAD.md's `slug:`, but close's resolution chain cannot reach a repo-root THREAD.md, so a pending doc in a tool repo never matches | This repo's `THREAD.md` has `slug: thread-skill`, but branch 1 only looks at `~/Projects/<Area>/<Project>/` and branch 2 only at `_shared/threads/<slug>.md`. Only the "this session wrote it" escape matches; a previous session's pending doc is invisible. |
| 5 | skills/close/SKILL.md:158 | Close writes `Read docs/handoffs/<doc> first` into THREAD.md Resume instructions; the consumer's close `git rm -f`s that doc and nothing clears the pointer | Session C runs `/thread:open <slug>`, follows the pointer, and tries to read a file that no longer exists. |
| 6 | skills/close/SKILL.md:195 | Step 8 caps the report at ≤12 lines but mandates one `handoff legacy:` line per legacy doc found — six already exist in the workspaces repo | Any close under `~/repos/workspaces` emits 6 fixed lines before the real rows. |
| 7 | skills/handoff/SKILL.md:80 | Handoff commits the doc with no repo-state guard while close's step 7.2 refuses to commit on detached HEAD or a half-applied git operation | A session on a detached HEAD commits the doc to an unreferenced commit; the next checkout drops it. § Withdrawn has the same hole. |
| 8 | skills/handoff/SKILL.md:86 | "If `git add` is refused because the path is ignored, fix the ignore in the same commit" directly contradicts close's Commit hygiene and edits the user's `.gitignore` unasked | Handoff in a deliberately-gitignored tree (`~/Projects/Tutorials/`, `_archive/`, `TSMS/`) rewrites `.gitignore`; close applies the opposite rule to the same artefact. |
| 9 | skills/handoff/SKILL.md:76 | Section reordering created a forward reference: § 6 must contain the paste-ready prompt verbatim and the doc must be committed "before anything else", but the prompt is defined in the *next* section | An agent executing top-to-bottom commits a doc whose § Paste-ready prompt is a placeholder; § Fail closed then says "write and commit" again and the second commit reads as a failure. |
| 10 | skills/close/SKILL.md:76 | `find` filters `node_modules` with `-not -path` instead of `-prune`, so it descends every excluded tree — and the scan runs twice per close | Measured: 4.5s wall / 14.1s system over `~/Projects` to return 1 result; `$top` is `~/Projects` for every project. |
| 11 | skills/close/SKILL.md:82 | The "consumed by this session" test is explicitly not on disk — "the marking is in this conversation" — breaking the ADR's decidability-after-compaction claim | After a compaction close cannot tell its own consumed doc from a dead session's; if anything commits the mark first, `git status` is clean and the doc is left `status: consumed` forever. |
| 12 | skills/close/SKILL.md:77 | The awk front-matter parser has no else-branch for an unrecognised `status:` value — anything but the bare tokens falls out of all three buckets silently | `status: "pending"` (quoted, valid YAML) prints `"pending"`; nothing matches; the doc is not refreshed, deleted, suppressing or reported. |
| 13 | skills/close/SKILL.md:87 | Rule 2 mandates a `refreshed: <today>` front-matter key that no other half of the contract defines | handoff and ADR 0017 specify exactly `thread:` / `written:` / `status:`. |
| 14 | CONTEXT.md:63 | Three inconsistent path forms for the same pointer — `<repo>/docs/handoffs/`, `<home>/docs/handoffs/`, and an undefined `<repo>` placeholder in the commit snippet | A consuming agent cannot tell whether the path it was handed is absolute, repo-relative or `<home>`-relative. |
| 15 | skills/close/SKILL.md:149 | Step 2 says "Run the three handoff checks" — a stale count from the reverted draft; the section now defines one `find` loop plus two prose filters | An agent following step 2 literally finds no three named checks. |

The engine emitted `file` / `line` / `summary` / `failure_scenario` only; no verdict or category.

## K27 classification and the stop

Findings 1, 4, 6, 7, 10, 11, 12 and 15 are about round-1's fixes (the `<home>` carve-out, the slug match, the legacy rule, the commit guard, the `find` scan, the "this session marked" filter, the awk parser, the rewritten step 2) — 8 of 15, 53% regressions, past K27's ~40% line. Per K27 the chain stops here: this round's findings are fixed below, no round 3 is dispatched, and the gate is the build's own verify list — the five-rep close rig (2 control / 3 treatment before round 1, 2 treatment after it, 2 more after this round; ADR 0017 § Decision records the result).

## Disposition (consumed 2026-09-21, same session)

| # | outcome | how |
|---|---|---|
| 1 | fixed | `<home>` is defined once in handoff § Where as the *unit directory*: the project directory under `~/Projects/`, the workspace directory under `~/repos/workspaces/`, else the CWD's git toplevel; else the session's workspace directory. Close's scan resolves the same `<home>`. |
| 2 | fixed | Line 14 names the third commit target: the handoff doc's own repo, one file by pathspec. |
| 3 | fixed | The Destinations row no longer claims "what is done"; it is category 3 only, matching rule 2. |
| 4 | fixed | Ownership is by location: a pending doc in `<home>/docs/handoffs/` is this thread's by construction; the `thread:` line only excludes a doc naming a *different* thread that `<home>` also carries. No resolution-chain dependency. |
| 5 | fixed | Step 4: when this session consumed the doc, the Resume pointer is replaced with real instructions; open § `<slug>`: a dangling pointer is reported and the brief comes from THREAD.md alone. |
| 6 | fixed | Legacy/unknown docs collapse to one report line with a count. With the scan now one directory, the count is small by construction. |
| 7 | fixed | Handoff gets the same guard: on a detached HEAD or half-applied operation the doc is written, not committed, and reported `not versioned:`; close's 7.2 `rm`s an untracked consumed doc and says so. § Withdrawn inherits the guard. |
| 8 | fixed | Never edit `.gitignore`: an ignored `<home>` falls back to the session's workspace directory, which is never ignored, and the prompt names that path. |
| 9 | fixed | § 6 says compose the prompt first (§ Build the handoff prompt) and the doc carries it verbatim; § Fail closed says the doc is *already* committed and only the prompt is printed. |
| 10 | fixed | The scan is `find "$home/docs/handoffs" -maxdepth 1 -name '*.md'` — one directory, no recursion, no exclusions needed. |
| 11 | fixed | Consumed is decided on disk alone: any `status: consumed` doc in `<home>/docs/handoffs/` is deleted at close, whoever marked it — consumed means the thread it briefed went live and history keeps the pending version. The "this session marked" filter and its `Needs your call:` edge case are gone. |
| 12 | fixed | The parser strips quotes and lowercases; anything other than `pending`/`consumed` reports as `unknown(<value>)` on the legacy line and is left alone. Verified on the quoted and capitalised fixtures. |
| 13 | fixed | `refreshed:` is in handoff's front-matter block as the optional key close adds; ADR 0017 names it. |
| 14 | fixed | One form everywhere: `<home>/docs/handoffs/<YYYY-MM-DD>-<slug>.md`, and the paste prompt's `Read first:` is the absolute path. |
| 15 | fixed | Step 2 says "run the handoff scan". |
