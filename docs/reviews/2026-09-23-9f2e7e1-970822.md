---
repo: /Users/lachlants/repos/tools/thread-skill/.claude/worktrees/enabler
head: 9f2e7e1
diff_digest: 970822
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-23
status: pending
---

Clean-room `code-review xhigh` of the uncommitted 2.5.3 diff (the execute § Worktree lifecycle
paragraph recommending a separate clone for a self-rollout, plus the manifest bump). 11 findings.

| # | file:line | summary | failure scenario |
|---|---|---|---|
| 1 | skills/execute/SKILL.md:414 | The separate-clone advice never says the clone's origin must be the GitHub remote; cloning the live checkout gives a local-path origin. | `git clone ~/repos/tools/thread-skill …`: the resolver returns the live checkout's branch, worktrees branch from its stale refs, implementers push into the live checkout and `gh pr create` fails, merge-wave cannot derive owner/repo and exits 2. Name `gh repo clone lachyts/thread-skill <path>`. |
| 2 | skills/execute/SKILL.md:414 | A separate clone does not stop plan-gate agents reading the live checkout: task notes cite absolute `~/repos/tools/thread-skill/...` paths and planners read "every source file it references". | Planners/judges read the live (held) checkout — stale-tree planning again; an implementer following a note's absolute path can edit the live checkout, which is live in every new session via the directory-source marketplace. |
| 3 | skills/execute/SKILL.md:414 | "Keep it on the base branch" is presented as sufficient, but only continuous mode runs merge-wave; in --gated / single-wave nothing fast-forwards repoPath. | --gated: the user merges wave 1 on GitHub; the wave-2 planner/judge/investigator read the pre-wave-1 checkout; same before wave 1 and on cold resume. Root fix: read-only agents read a fresh detached `origin/<base>` worktree instead of the mutable checkout. |
| 4 | skills/execute/SKILL.md:414 | The clone replaces one manual convention with another: schedule fills Project root from the project note's `Local:` line (the live checkout) and nothing detects a self-rollout against it. | Operator doesn't hand-edit Project root: the rollout runs against the live checkout on master and each wave goes live mid-rollout. Deeper fix: execute warns/refuses when repoPath is the directory-source marketplace path, or schedule takes a declared repo-path override. |
| 5 | skills/execute/scripts/merge-wave.sh:110 | The log still calls a non-base skip "harmless", and comments (:86-91, :448) still call the refresh cosmetic and the skip the self-rollout lever — the new SKILL.md text says the opposite. | An operator parked on a feature branch reads "(harmless)" and continues; wave 2 plans against a stale tree; the comment at :88-90 still teaches the removed hold lever. |
| 6 | skills/execute/scripts/merge-wave.sh:106 | A failed fast-forward (dirty/diverged checkout on the base) only warns and still writes `ok`, though the new doc makes that fast-forward matter for correctness. | Uncommitted edit in a file wave 1 touched → ff refused → WARN, `ok`, cursor advances → wave 2's read-only agents read the pre-wave-1 tree. |
| 7 | skills/execute/SKILL.md:414 | "Pull the live checkout at release" omits the version bump + plugin update; the engine/merge-wave/hook run from the version-keyed cache. | After a self-rollout the operator pulls: new sessions read new SKILL.md prose while `${CLAUDE_PLUGIN_ROOT}` runs the old engine — the 1c87bd4 #4 split. Step should be: bump both manifests, pull, `claude plugin update thread@thread`, `make release-check`. |
| 8 | skills/execute/SKILL.md:414 | Names only the planner and investigator as reading the checkout; the plan judge, plan reviser and review judge are also handed `Project root: ${a.repoPath}` and read it. | A future fix repoints only planner + investigator at a fresh snapshot; the plan judge still checks plans against the stale tree (wasted rounds, false plan-blocked). |
| 9 | skills/execute/SKILL.md:414 | Doesn't say where the clone should live; the daily reaper scans only `~/repos/**/.claude/worktrees/`. | Clone under ~/Projects, scratchpad or /tmp: audit-fix worktrees never reaped; /tmp can vanish mid-rollout. Pin it under ~/repos. |
| 10 | skills/execute/scripts/merge-wave.sh:131 | `--self-test-base` still labels the hold-branch skip as the supported lever, and the audit doc (:107-108) says it "proves a root parked on a hold branch is never fast-forwarded". | A maintainer reading the labels or the audit reintroduces `git switch -c hold/<version>` for a self-rollout; planners then plan against the pre-rollout tree. |
| 11 | .claude-plugin/plugin.json:3 | With the bump to 2.5.3, the e2e checklist contradicts itself: frontmatter `plugin: 2.5.2` and intro "on the 2.5.2 plugin" vs setup row "Fresh session on 2.5.3+". | Results recorded under the wrong version; the 2.6.0 release gate can't trace a regression to its version. |
