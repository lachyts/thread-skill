---
repo: /Users/lachlants/repos/tools/thread-skill
head: 026dd42
diff_digest: 20344a
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-26
status: pending
---

Target: `e886923...026dd42` (p5-2, branch `p5-2/snippets-to-scripts`). Cell `o48-xhigh-v1`: no verify
phase, so findings are ungraded (no verdict or category from the engine).

| # | file:line | summary | failure scenario |
|---|---|---|---|
| 1 | skills/open/SKILL.md:90 | Lookup wrapper now depends on `${CLAUDE_PLUGIN_ROOT}`, but close's rungs 2/3 and step 7.1 read open's SKILL.md as a file (not substituted) and the Bash tool leaves it unset | Rung 3 runs the lookup → `lk=/skills/open/scripts/repo-thread-lookup.sh` → exit 2 "script not found"; the old snippet exits 0 in the same shell — a regression |
| 2 | skills/close/SKILL.md:25 | No caller of the lookup handles exit 2; a failed lookup is indistinguishable from "no hit" | Lookup exits 2 with empty stdout; close falls through to rung 4/5, skips the repo THREAD.md update, step 7.1 skips its commit; open `<slug>` may create a duplicate thread |
| 3 | skills/close/SKILL.md:71 | Logic moved from tree-read SKILL.md text into version-keyed-cache scripts; wrapper text and script logic now ship on different channels | A session after 3c7280e but before a plugin update calls scripts absent from the 2.7.0 cache; a same-version script fix never reaches the cache |
| 4 | tests/repo-threads.test.sh:96 | All new tests inject `CLAUDE_PLUGIN_ROOT` as exported env, which never matches real use | No test runs a wrapper with the variable unset and the script present; close rung 3 losing its lookup passes `make test` |
| 5 | skills/close/SKILL.md:163 | repo-track, lookup and default-branch now need `${CLAUDE_PLUGIN_ROOT}` substitution; Codex never substitutes | Codex pastes the wrapper, the variable is empty, exit 2 — the same paste worked before |
| 6 | skills/open/scripts/repo-thread-lookup.sh:1 | No `.gitattributes` forcing LF on `*.sh` | `core.autocrlf=true` checkout → CRLF scripts → `$'\r': command not found` under Git Bash |
| 7 | skills/close/scripts/handoff-scan.sh:26 | `bash <script>` sources `BASH_ENV`; only grep is hardened | A `BASH_ENV` defining `find()` or printing output corrupts the stdout line contracts |
| 8 | skills/close/SKILL.md:167 | The `command grep` rationale blames the zsh snapshot function, which cannot reach a fresh bash | Misdirects maintainers away from the real vector (`BASH_ENV` / exported functions) |
| 9 | skills/open/SKILL.md:86 | Section still says flatly "writes nothing to stderr and exits 0" before adding the exit-2 case | A caller reading the first sentence treats exit 2 + empty stdout as an ordinary empty result |
| 10 | THREAD.md:674 | Known-quirks workaround (run the snippet from source with `sed`) is now wrong | The extracted wrapper needs `CLAUDE_PLUGIN_ROOT`; following it gives exit 2 "scan failed" |
| 11 | skills/execute/SKILL.md:183 | Exit 2 is documented as "script not found" but the script also exits 2 on a usage error | A bad invocation is reported as a missing script |
| 12 | README.md:145 | README's contracts list omits `tests/contracts/skill-positional-args.test.mjs` | A contributor adding a positional to a SKILL.md learns of the ban only from CI |
| 13 | skills/open/scripts/repo-thread-lookup.sh:13 | `fms()` and `thr()`/`cls()` are the same front-matter parser, copy-pasted | A parser fix must land in three places |
| 14 | skills/close/SKILL.md:73 | Four wrappers repeat the same exists-check boilerplate; scripts re-default inputs the wrappers already defaulted | Four guards to keep in sync, already drifting in wording and exit-code docs |
