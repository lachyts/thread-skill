---
repo: /Users/lachlants/repos/tools/thread-skill
head: ed4b2b4
diff_digest: e65597
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-21
status: pending
---

| # | file:line | summary | failure scenario | verdict | category |
|---|---|---|---|---|---|
| 1 | skills/close/SKILL.md:164 | Prescribed close-out `git rm docs/handoffs/<doc>` fails on every consumed doc, because the pickup edit `status: consumed` sits uncommitted in the working tree. | Verified in a scratch repo: commit a doc with `status: pending`, sed it to `status: consumed`, then `git rm docs/handoffs/x.md` → `error: the following file has local modifications`, exit 1. Every normal consume→close cycle aborts at step 7.2, the doc is never deleted, `docs/handoffs/` accumulates, and the "no buildup" claim at handoff:90 never holds. Needs `git rm -f` (or `git add` before `git rm`). | — | — |
| 2 | skills/close/SKILL.md:76 | Detection globs only `$top/docs/handoffs/*.md` (one directory), but handoff:56 writes to `~/repos/workspaces/<workspace>/docs/handoffs/`, a subdirectory of the workspaces repo. | On disk now: 3 docs in `~/repos/workspaces/docs/handoffs/`, 2 in `2d-workspace/docs/handoffs/`, 2 in `ops-workspace/docs/handoffs/`. A close run anywhere under `~/repos/workspaces` resolves `$top=/Users/lachlants/repos/workspaces` and globs only the root dir — 4 of 7 existing docs are invisible. A 2d-workspace session that just wrote a handoff then closes sees no pending doc and proposes the exact duplicate vault tasks ADR 0017 exists to prevent. The comment "# every handoff in the tree" is false: a single-level glob is not a tree walk. | — | — |
| 3 | skills/close/SKILL.md:75 | `top="$(git rev-parse --show-toplevel 2>/dev/null)"` silently yields the empty string outside a repo, making every subsequent path `/docs/handoffs/*.md`; the prose fallback at line 70 is never encoded in the snippet. | Session runs with CWD `~/Projects/Animately/giflab/` (not a git repo) — exactly the case handoff:56 names for the workspace-repo fallback. `git rev-parse` fails, `$top=""`, the checks probe the filesystem root, find nothing, and close reports no pending handoff. The doc handoff just wrote is orphaned and the continuation is proposed as vault tasks anyway. Same silent-empty failure for a detached-HEAD or bare-repo CWD. | — | — |
| 4 | skills/close/SKILL.md:70 | No migration for pre-existing handoff docs: none of the 7 docs already on disk have YAML front matter, so `grep -L '^status: consumed'` classifies every one as permanently pending. | `head -8` on all 7 existing docs shows each starts with `# Handoff — …`, no `---` block. ADR 0017 rules "treat any doc that does not say `status: consumed` as pending" but supplies no backfill. `~/repos/workspaces/docs/handoffs/2026-09-19-session-a-findings-consumed.md` is already consumed (its own title and body say so) yet is now pending forever. Every close in the workspaces repo prints handoff rows for 3 zombie docs and risks folding real continuation into one of them. | — | — |
| 5 | skills/handoff/SKILL.md:90 | Delete-at-consumer's-close assumes exactly one consumer; the repo's own handoff docs record that assumption already failing, and the new design reproduces it. | `~/repos/workspaces/docs/handoffs/2026-09-19-memory-and-model-defects.md` states verbatim: "this file is kept alive for it rather than deleted as originally instructed: the delete-on-consume rule assumed one consumer, and deleting it now would strand Session B's brief." Under the new rule Session A marks it consumed, closes, and the doc is `git rm`'d — Session B cold-starts with a dangling `Read first:` path and must dig it out of git history. ADR 0017 § Rejected never considers the fan-out case. | — | — |
| 6 | skills/close/SKILL.md:83 | A pending handoff doc has no expiry, so one abandoned or misfired handoff suppresses this thread's vault-task proposals in that repo indefinitely. | handoff:23 concedes the route is model-invocable and can misfire; the doc is committed "before anything else" (handoff:80). User says "actually never mind, keep going" — nothing marks it consumed, no native task exists to pick it up. Every subsequent close on that thread finds it pending, folds all continuation into it via rule 2, and proposes zero tasks. No staleness/TTL rule and no `written:`-date check exists, so the suppression is permanent rather than the "one committed doc" handoff:23 claims. | — | — |
| 7 | CONTEXT.md:75 | The glossary's Pickup entry now claims pickup consumes a handoff doc, but `thread:open` — the pickup route — was not changed and still only handles the stash/defer capture task. | `skills/open/SKILL.md:49-56` is titled "`/thread:open [[<task>]]` — pick up a stashed/deferred capture", step 3 marks only a *task* `status: done`, and line 103 guards only that. The consumed-marking lives solely inside handoff's paste prompt. A user who resumes the handed-off thread via `/thread:open <slug>` never marks the doc consumed → it stays pending forever (finding 6's permanent suppression), and the doc is never deleted. | — | — |
| 8 | skills/close/SKILL.md:164 | Step 7.2's consumed-doc deletion is unqualified ("A consumed doc: `git rm` …"), contradicting line 89 and edge case line 211, which put a consumed doc this session did not mark out of scope. | The step-2 check `grep -l '^status: consumed'` returns *all* consumed docs in the repo, not just this session's. A session that dies after marking a doc consumed leaves it behind; the next unrelated close in that repo reads step 7.2 literally and `git rm`s another thread's in-flight brief — while lines 89 and 211 say it should instead emit one `Needs your call:` line. Two rules, one artefact, opposite actions. | — | — |
| 9 | skills/close/SKILL.md:76 | The documented detection snippet is bash-shaped and breaks under zsh, the user's shell: an unmatched glob is a fatal error that prevents the command from running at all. | Verified: `zsh -c 'ls "$top"/docs/handoffs/*.md 2>/dev/null'` on a repo with no handoffs dir prints `zsh:1: no matches found: ./docs/handoffs/*.md` (the `2>/dev/null` does not suppress it — the shell, not the command, emits it) and exits 1 without running `ls`. Bash silently returns exit 2. The common no-handoffs case emits three spurious errors into the close transcript under zsh, and since the commands never execute nothing distinguishes "no handoffs" from "check failed". Needs `setopt null_glob` / `nullglob`, or `find`. | — | — |
| 10 | skills/handoff/SKILL.md:62 | `thread: <THREAD.md slug if the thread has one, else this handoff's slug>` breaks close's ownership test for project threads, which are identified by directory and have no slug in close's resolution chain. | close:70 says a doc is this thread's "when its `thread:` line names the active thread", and ADR 0017 adds "a `thread:`-less doc is matched to the active thread by its title". For a project THREAD.md (close's branch 1: CWD inside `~/Projects/<Area>/<Project>/`) handoff falls through to "this handoff's slug" — the doc carries a `thread:` line that does NOT name the active thread and is not `thread:`-less, so neither branch matches. The pending doc is treated as another thread's and close proposes the duplicate tasks. `~/.agents/skills/thread/THREAD-template.md` does carry `slug:`/`scope:` frontmatter, but close's branch 1 never reads it. | — | — |
| 11 | skills/close/SKILL.md:61 | The Destinations table's "Each candidate lands in exactly one of these" is still violated: scan categories 1–3 now feed both the THREAD.md row and the new handoff-doc row. | Row 3 claims "where we left off, what shifted, new decisions"; row 4 claims "what is done, what remains, the next move"; rule 2 at line 84 says to "compare it with categories 1–3 of the scan" and write into the doc; line 91 says "THREAD.md is updated as normal". Category 1 therefore has two destinations and the close agent must guess. This is the prior review's finding 6, which ADR 0017 § requirement 4 claims to have resolved — it resolved only the *Resume instructions* sub-case. | — | — |
| 12 | skills/close/SKILL.md:164 | close now auto-commits to an arbitrary repo (`$top`) — previously it only ever committed `~/repos/workspaces` and the vault — with no branch check, no dirty-index edge case, and no ADR discussion of the widened blast radius. | Session working in `~/repos/animately/giflab` on `master` mid-feature: close silently creates a commit in that production repo (and handoff:82 creates a second one). The user-level `~/.claude/CLAUDE.md` git rule states "Commit or push only when the user asks. If on the default branch, branch first." — neither is honoured. close's edge-case list covers "Not inside `~/repos/workspaces/`" only for the workspaces commit step, and has no case for a repo mid-rebase, in detached HEAD, or with a gitignored `docs/` path (handoff:84 handles that; close does not). | — | — |
| 13 | skills/handoff/SKILL.md:23 | The invocation-gate rationale understates the new misfire cost: since ADR 0017 a misfired handoff silently disarms the next close's vault-task menu for that thread. | The gate's stated justification for staying model-invocable was "it writes nothing durable"; the diff replaces that with "a misfire costs one committed doc, undone with `git rm` in the next commit" while leaving model-invocability on. The uncounted cost is close § The handoff owns the continuation: a stray pending doc removes the thread's entire continuation from the proposal set (finding 6). The gate's cost/benefit was re-derived from an incomplete cost, so the "cheapest-misfire route of the family" claim no longer follows from its own premise. | — | — |
| 14 | docs/adr/0011-close-saves-autonomously-vault-tasks-stay-gated.md:4 | ADR 0017 amends 0011 but 0011 carries no amendment note, against the convention ADR 0015 set and 0012/0014 follow. | ADR 0015:84 states the rule explicitly — "Both carry the amendment note" — and 0012:41/0012:65/0014:31/0014:36 all carry inline `*(Amended by ADR 00NN, <date>: …)*` markers. 0011's `Status:` line and its § Rejected "Fully autonomous including vault tasks" bullet are both materially narrowed by 0017 and both read as unqualified. A future reader landing on 0011 (cited by close at lines 14 and 217) gets the superseded rule with no forward pointer. | — | — |

Note: the engine returned its findings as structured JSON carrying `file` / `line` / `summary` / `failure_scenario` only — no `verdict` or `category` field. Those two columns are left as `—` rather than invented.
