---
repo: /Users/lachlants/repos/tools/thread-skill
head: 04be604
diff_digest: 8f929f
harness: claude
engine: code-review
mode: code
effort: high
reviewed: 2026-09-15
status: pending
---

# Clean-room review — round 3

| # | file:line | summary | failure scenario | verdict | category |
|---|---|---|---|---|---|
| 1 | `skills/stash/SKILL.md:18` (same text at `skills/defer/SKILL.md:19`) | The new "One filesystem gate" mis-routes project observations during normal dev work | `/thread:stash` run from `~/repos/animately/giflab` fails the "CWD inside `~/Projects/<Area>/<Project>/`" gate, so a giflab-only arrangement observation is pushed up to the animately-workspace seat or estate ledger instead of the existing `~/Projects/Animately/giflab/METHOD.md` — the exact over-routing ADR 0015 says nothing corrects. | HIGH (severity, not a CONFIRMED/PLAUSIBLE verdict — see note) | not emitted |
| 2 | `skills/close/SKILL.md:38` | `close` lost its scope gate, so the three writers now disagree on the same input | From a workspace or tool-repo CWD with a vault project note in play, `/thread:close` may create and write `<project>/METHOD.md` derived from the note while `/thread:stash` is explicitly forbidden to; and a shared-thread close that is "unsure" is now told to route to a project that does not exist. | MEDIUM | not emitted |
| 3 | `skills/stash/SKILL.md:18` / `skills/defer/SKILL.md:19` | The gate cites a rule `close` no longer has | An agent told to route ledgers "per `close`'s identification branch 1" reads `skills/close/SKILL.md:25`, finds only THREAD.md selection and no ledger rule, and improvises the destination; the same sentences restate a rung of the test while asserting they do not. | MEDIUM | not emitted |
| 4 | `docs/windows-setup.md:17` | The "NOOPs at every altitude on Windows" guarantee has no mechanism left | A Windows session with the workspaces clone present follows stash/defer's "run the scan on every stash / every defer", cannot read the routing test (`~/.agents` absent), and hand-rolls an estate `METHOD.md` row — precisely what windows-setup forbids; no skill body carries the Windows conditional and the doc is not loaded at runtime. | MEDIUM | not emitted |
| 5 | `docs/adr/0014-stash-and-defer-run-the-silent-process-scan.md:32` | The amendment is incomplete; the superseded destination survives unmarked | A reader landing on 0014's next bullet ("A hit appends to the project's `METHOD.md` `## Candidates`") or on line 27 gets the pre-0015 project-only destination with no amendment marker, and routes every candidate to the project ledger. | LOW | not emitted |
| 6 | `skills/close/SKILL.md:54` | The seat path enumerated in the Destinations row omits sub-seats | A seat-altitude row captured in an SEO session lands in `~/repos/workspaces/animately-workspace/knowledge/METHOD.md` instead of the declared sub-seat `~/repos/workspaces/animately-workspace/seo/knowledge/METHOD.md`, because line 54 is the row an agent copies the concrete path from. | LOW | not emitted |
| 7 | `skills/close/SKILL.md:54` | "`K` + the next free ordinal in that ledger" is ambiguous and can collide | In a ledger already holding `K01`, `K02`, `M01…M21`, `U01…U07`, one agent reads "next free ordinal" as the next free number and writes `K22` while another writes `K03` for the same new row. | LOW | not emitted |
| 8 | `CONTEXT.md:105` | Cross-repo glossary divergence from adding `[correction]` on one side only | An agent resolving Seat/Estate status vocabulary through the delegation at lines 90/93 reads `~/repos/workspaces/_shared/CONTEXT.md` § Method's five tokens and treats a `[correction]` row as invalid. | LOW | not emitted |

## Verified clean (engine's own list)

- `.claude-plugin/plugin.json` / `marketplace.json` version bump to `2.3.5` is consistent across both; no other in-repo version references.
- Every new cross-reference resolves: method skill `### Which ledger a row goes to`, `~/repos/workspaces/_shared/CONTEXT.md` § Method, `_shared/docs/adr/0003-method-at-every-altitude.md`, `METHOD-template.md` (does support seat/estate frontmatter), README's ADR 0013/0014/0015 titles, and `knowledge/processes.md` genuinely no longer existing.
- The new `Commit hygiene` incantation is correct — tested `git add <path> && git commit -m "…" -- <path>` against an untracked file with unrelated pre-staged work: only the named path is committed, pre-staged file untouched and still staged.
- `close`'s row form now matches the method contract's `<ID>` / `path § heading` shape, and step 7.3's "sub-step 6" ordering reference is accurate.

## Run provenance

- Level `high` was passed to the engine, but the profile that actually ran was the engine's
  `minimal prompt → single careful diff pass → ≤15 findings` profile.
- The engine forked itself into a background `code-review` agent
  (`agent-abba913190eed6ad3`, spawnDepth 2). That agent used only `Bash` and `ToolSearch`,
  never called `Agent`, and has no child agents — so the multi-agent fan-out did **not** run.
  The review degraded to a single inline pass.
- `ReportFindings` was absent in the forked environment, so the engine reported inline. That
  profile emits a severity per finding and no `CONFIRMED`/`PLAUSIBLE` verdict and no category;
  those two columns carry the engine's severity and `not emitted` rather than invented values.
- `effort: high` in the front matter above is left as pre-filled, per the dispatch contract.
