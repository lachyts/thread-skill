---
repo: /Users/lachlants/repos/tools/thread-skill
head: c9f09dd
diff_digest: df669e
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-21
status: consumed
---

# Review — the round-3 fix batch (`07ddc75..c9f09dd`)

Clean-room `xhigh` pass over the committed range `07ddc75..c9f09dd`, excluding `docs/reviews/`.
Resolved scope: 5 files, 143 insertions, 16 deletions — matches the brief. The working tree was
clean and pushed, so an explicit range target was required; a bare `git diff` would have returned
nothing.

**This round reviews the previous round's fixes.** Ten of the fifteen findings cite lines this
chain itself added. See § Stop-rule assessment below — that is the signal § Rounds exists to catch,
not an incidental fact.

**Verdict/category caveat.** The engine emitted `file`/`line`/`summary`/`failure_scenario` only, as
at this cell it has no verify phase. The relaying agent filled `verdict` and `category` from each
finding's own text rather than leaving them `not emitted`. They are therefore the agent's
characterisation, not the engine's, and are reproduced here marked as such.

## Findings

| # | file:line | summary | failure scenario | verdict* | category* |
|---|---|---|---|---|---|
| 1 | `skills/execute/wave-execute.workflow.js:1058` | The new retry-budget floor cancels the halving it guards: at `max_iterations` 2 the capped retry gets a FULL second budget (2x total, the exact doubling the split exists to prevent), and at 1 it still gets the degenerate 1-iteration Ralph loop the floor was added to forbid. | Probe-confirmed by driving `converge()` with `maxTier:'opus'` at each n. `retryBudget = Math.min(n, Math.max(2, Math.floor(n/2)))`: n=1 → retry 1 (total 2 = 2.0x; the retry runs `ralphLoop` step (d) at i==1, committing an unverified fix — the precise degradation the new comment says the floor prevents); n=2 → retry 2 (total 4 = 2.0x, a second FULL budget); n=3 → 5 (1.67x); n=4 → 6 (1.5x). An operator who lowers `max_iterations` to 2 to economise under an exhausted quota gets the full 2x verifier spend back, silently. The engine comment ("Capped worst case is therefore ~1.5x max_iterations") and the test label at `tests:348` ("never a second full one") are both false for n≤2. | Confirmed (probe) | bug |
| 2 | `skills/execute/tests/prompt-invariants.test.mjs:348` | The whole retry-budget expression is unguarded by the suite — three separate mutations of the changed line, including one that halves a REAL fable escalation's budget, all pass ALL 184 assertions. | Mutation-run against the committed suite: (a) `const retryTask = { ...task, maxIterations: retryBudget(...) }` — drops the `moved ?` guard so a genuine uncapped opus→fable takeover gets HALF the iteration budget instead of the full one the escalation exists to buy → ALL PASS; (b) `const retryBudget = (n) => 2` → ALL PASS; (c) `const retryBudget = (n) => Math.max(2, Math.floor(n/2))` (drops the `Math.min` ceiling) → ALL PASS. Scenarios E (n=4) and F (n=3) both happen to land on 2, so the constant is pinned and the half/floor/min semantics and the escalated-path exemption are not. A refactor that cuts an escalated fable task's budget in half ships green. | Confirmed (mutation) | test-gap |
| 3 | `skills/execute/wave-execute.workflow.js:819` | The rewritten `escalate()` comment asserts a false safety property about the builder it names: `plannerPrompt` compiles fine without `st` and silently renders the WRONG framing — the exact omission class the comment claims is unwritable. | Probe-confirmed in the vm sandbox: `plannerPrompt(tk, a, undefined, 'prior diag')` returns a complete prompt containing "SECOND PASS" and no "STRONGER-TIER" — no throw. Same for `readOnlyPrompt`. Only `implementerPrompt`/`approvedPlanImplementerPrompt` throw, because they reach `verifyBlock → terminalTier(st.tier)`; `plannerPrompt`'s only `st` consumer is `escalationContext(prior, st)`, whose `!(st && st.escalated)` guard treats `undefined` st as "not escalated". So a future builder that forgets `st` produces a genuinely-escalated fable agent told it is a same-tier second pass — the original `plannerPrompt` bug, reproduced silently. Same defect class (a false load-bearing comment) that round-3 finding 3 was fixing, reintroduced by its fix. | Confirmed (probe) | bug |
| 4 | `skills/execute/wave-execute.workflow.js:1050` | The superseded comment paragraph was left in place above the new one: lines 1050–1052 repeat the new paragraph's opening sentence verbatim and end with the now-false conclusion "The two passes SHARE the budget instead." | Reading `implement()` at HEAD shows two consecutive comment blocks both opening "Under a cap the first pass was ALREADY terminal, so it spent a full Ralph budget." — the first concluding the passes SHARE one budget (total = n), the second concluding ~1.5 × n. They cannot both be true and the first is the one the code no longer does. The review disposition for finding 5 claims "'SHARE' replaced with 'a REDUCED budget'", but the replacement happened only in the test label at `tests:348`; the engine comment still says SHARE. | Confirmed | doc-drift |
| 5 | `docs/adr/0016-a-tier-ceiling-caps-escalation-when-quota-is-gone.md:36` | ADR 0016 § 2 still documents the event-flag rule this commit proved wrong ("Once a cap suppresses an escalation, the task takes the higher tier's effort row"), so the decision record the code cites now contradicts the code. | `effortTier`'s new comment cites "(ADR 0016)" as its authority while doing the opposite of what ADR 0016 says: effort now follows terminality from the FIRST dispatch, before any suppression. `CONTEXT.md:244` and `execute/SKILL.md:83` were already terminality-framed; the ADR was not updated in this range. A maintainer resolving a future disagreement goes to the ADR (the repo's own doc hierarchy) and restores the `capSuppressed` rule — reintroducing the exact bug Scenario F exists to catch. The ADR also still says nothing about the capped retry's budget split, now a load-bearing consequence of the ceiling. | Confirmed | doc-drift (out of range) |
| 6 | `skills/schedule/rollout-template.md:77` | The new resource-budget figure "~1.5 × max_iterations" is wrong at the template's own default and worst at the small values an operator under a quota cap is most likely to pick. | Measured from `converge()`: the capped implement layer costs `n + min(n, max(2, floor(n/2)))` against an uncapped `1 + n`. n=3 (this template's own `max_iterations: 3`) → 5 vs 4 = 1.67x, not 1.5x; n=2 → 4 vs 3 = 2.0x; n=1 → 2 vs 2 = 2.0x. The figure is only accurate for n≥4. This text is copied verbatim into every generated rollout note, so an operator sizing a capped wave from it under-budgets the implement phase at the default configuration. | Confirmed (measured) | doc-drift |
| 7 | `skills/execute/SKILL.md:83` | The `max_tier` config row still advertises "(`opus` \| `fable`)" as a two-value menu — the exact wording the round-3 review named as needing the fix, patched in the template only. | Round-3 finding 11 says verbatim: "execute/SKILL.md:83 carries the same wording, so the diff propagates an existing wart into a user-facing artefact." The template was corrected to "`opus` is the ONLY value that caps anything — `max_tier: fable` is a no-op"; SKILL.md:83 was not. SKILL.md is the file the lead agent reads to resolve rollout frontmatter into args, so the agent-facing surface still presents a 50/50 menu in which one option silently produces an uncapped run with no log line (`tierCap` returns `TOP_TIER` and the startup guard only fires when `tierCap(a) !== TOP_TIER`). The two documents now disagree. | Confirmed | doc-drift (out of range) |
| 8 | `skills/execute/wave-execute.workflow.js:870` | Two of the three sub-expressions in the new `effortTier` predicate are noise: `&& st.tier !== TOP_TIER` is dead logic, and `st.capSuppressed \|\|` is production-unreachable, kept alive only by two under-specified test doubles. | Mutation-confirmed: replacing the body with `return (st.capSuppressed \|\| terminalTier(st)) ? TOP_TIER : st.tier` passes ALL 184 assertions — when the condition is true and `st.tier` is already `TOP_TIER`, both branches return the same `'fable'`. Separately, `st.capSuppressed` is only ever written inside `escalate()`'s `if (terminalTier(st))` branch and terminality is monotone (the tier never moves under a cap), so `capSuppressed` always implies `terminalTier` in production; dropping that disjunct fails exactly two assertions, `tests:218` and `tests:219`, whose state doubles `{ tier: 'opus', capSuppressed: true }` omit `cap` and therefore model a state `converge()` cannot produce. Engine code is being shaped to satisfy a malformed test double — the comment admits it ("capSuppressed is kept in the test"). Correct form: `terminalTier(st) ? TOP_TIER : st.tier`, plus `cap: 'opus'` on the two doubles. | Confirmed (mutation) | cleanup |
| 9 | `skills/execute/tests/prompt-invariants.test.mjs:217` | The comment and assertion label above the helper-level effort assertions still teach the superseded event-flag contract that the engine change just replaced. | Line 217 reads "Effort is not quota-scarce: once an escalation is suppressed the task takes the higher tier's row" and line 218's label reads "maxTier: suppressed escalation keeps the higher tier EFFORT row". Both encode "effort changes when suppression happens", which is precisely the rule Scenario F was added to disprove ("effort follows terminality, not the `capSuppressed` event"). The test file now states both contracts in one place, 150 lines apart, with the wrong one in the position a reader hits first. | Confirmed | doc-drift |
| 10 | `docs/reviews/2026-09-17-f4ba792-round2.md:176` | The correction block published to stop future readers believing wrong numbers publishes the pre-fix arithmetic as the corrected record. | It states "The capped implement layer spends `n + floor(n/2)` ~ 1.5 × max_iterations … still more than uncapped for n ≥ 4", then in the next sentence says the budget now floors at 2. Post-fix the formula is `n + min(n, max(2, floor(n/2)))`, which exceeds the uncapped `1 + n` for every n ≥ 2, not only n ≥ 4, and at the template default n=3 gives 5 rather than the 4 the quoted formula yields. The sentence describes the code as it was before the fix it is announcing. | Confirmed | doc-drift (excluded path — reviewer did not read the file) |
| 11 | `skills/execute/tests/prompt-invariants.test.mjs:369` | Scenario F omits the two assertions that would pin the structural fact it exists to cover — that the cap first bit at the implement layer, with no plan layer, and that the ceiling held on every dispatch. | Scenario E asserts both `tierCappedAt === 'plan'` and `effortCalls.every(c => c.model === 'opus')`; Scenario F asserts neither, checking only `tierCapped === true && escalated === false`. Probe confirms the distinguishing value is `tierCappedAt === 'implement'`. Without it, a regression that moved the suppression stamp (or that let a judge dispatch escape the ceiling on the non-plan-gated path, which Scenario E structurally cannot see because it has a plan layer) leaves Scenario F green. | Confirmed (probe) | test-gap |
| 12 | `skills/status/SKILL.md:1` | `/thread:status` and `/thread:repair` never mention `max_tier` or `tier_capped`, although `execute/SKILL.md:270` and ADR 0016 § 3 both make those two skills the consumers of the marker. | `grep -rn 'max_tier\|tier_capped' skills/` matches only schedule (template + SKILL), execute (workflow, SKILL, tests) and `reconcile-wave.py`. ADR 0016 § 3's whole justification for the durable stamp is that "/thread:status and /thread:repair build their triage from note frontmatter"; `execute/SKILL.md:270` says "never let /thread:repair read it as input-gated". Neither conductor's instructions mention the key, so a capped rollout's blocked tasks get triaged by `/thread:repair` as genuine walls — the ADR 0016 failure mode — and this diff propagated the ceiling contract into schedule and the operator-facing template while stopping short of the two skills the ADR names. | Confirmed (grep) | doc-drift (out of range) |
| 13 | `THREAD.md:278` | The plugin-loading correction states the mechanism but omits the hazard it creates: if a session loads skills from the working tree, then UNCOMMITTED, half-finished edits to any SKILL.md are live in every new session across the estate. | The old version-keyed cache was an accidental safety barrier — nothing shipped until two manifests were bumped and the plugin updated. The corrected note records the loss of the cache dance as pure upside ("a committed change is live in the next session with no cache dance at all") and adds only the `${CLAUDE_PLUGIN_ROOT}` caveat. Mid-edit state in this repo — e.g. a SKILL.md being rewritten, or a syntax-broken workflow.js — is now loaded by any session started during the edit, with no stated guard (stash before stepping away / land edits in one write). This session itself began with four modified files in the tree. | Judgement | doc-drift |
| 14 | `skills/execute/wave-execute.workflow.js:1141` | A capped run that lands green on its first pass reports `tierCapped: false`, so nothing on the task note or in the result records that the task ran under a ceiling. | Probe-confirmed: `converge({scope:'single-file', planGate:false}, {maxTier:'opus'})` with a green first implement returns `tierCapped=false`, `model='opus'`. `escalate()` is never called, so `capSuppressed` is never set, so reconcile stamps no `tier_capped`. The task ran a full Ralph loop at the fable EFFORT row on the opus model — a materially different run profile from an uncapped opus task — and the note is indistinguishable from an ordinary uncapped opus success. Auditing which tasks in a rollout ran ceilinged is impossible after the lead session ends. | Confirmed (probe) | bug |
| 15 | `skills/schedule/rollout-template.md:77` | The capped-cost sentence models only `implement()`, but `reviserPrompt` hard-codes a FULL `ralphLoop(task.maxIterations)` for every review round, so the per-task capped total is far above the stated figure. | `wave-execute.workflow.js:669` renders `ralphLoop(task.verifier \|\| a.verifier, task.maxIterations, ...)` with the unreduced budget, and `reviewLoop` can dispatch up to `maxReviewRounds-1` revisers (default 4 → 3). A capped task that converges only at the review ceiling therefore spends `n + 2 + 3n` verifier iterations, not the ~1.5n the new sentence puts in front of the operator, and the sentence sits inside the paragraph whose headline formula ("Worst-case per task is max_iterations × verifier-time × max_review_rounds") is explicitly per-task, inviting the reader to substitute 1.5n for n there. | Confirmed | doc-drift |

\* `verdict` and `category` are the relaying agent's characterisation, not engine output — see the caveat above.

## Scope notes

- All 15 paths exist inside the repo.
- **Four findings cite files outside the reviewed range.** #5 (ADR 0016), #7 (`execute/SKILL.md`)
  and #12 (`status/SKILL.md`) are untouched by the range — flagged as consistency drift *caused by*
  the in-range change, which is legitimate but means they are not diff lines. #10 cites the
  round-2 review doc, which is in the raw commit range but on the excluded `docs/reviews/` path;
  the reviewer did not read it and could not corroborate that finding itself.
- The range contains 4 commits (`c9f09dd`, `00d17cb`, `65b8839`, `4d523aa`); the three older ones
  touch only `docs/reviews/`, so with that path excluded the diffstat matches the brief.

## How this review ran

- Claude Code (Opus 5), built-in `code-review`, `xhigh`, invoked as
  `Skill(code-review, "xhigh 07ddc75..c9f09dd")`. The engine forked once and returned its result
  synchronously through the wrapper's Skill call. The wrapper spawned zero Agent calls itself and
  makes **no claim of multi-agent fan-out** beyond that single fork.
- The engine ran live vm-sandbox `converge()` probes and mutation tests against the 184-assertion
  suite, and reported restoring every mutation. Working tree verified clean, HEAD unmoved, stash
  list empty.

## Disposition — consumed 2026-09-21

**13 fixed, 2 deferred.** Nothing marked deliberate. The two deferred are pre-existing gaps this
chain did not introduce, recorded in THREAD.md § Open questions so they survive this doc's deletion.

### Stop-rule assessment — this chain is now CLOSED

`review-ledger.py` fired **STOP** on this round:

```
rnd  head      mode       n  ORIG  REGR  UNK   %regr
  1  07ddc75   code      12    12     0    0    0.0%
  2  c9f09dd   code      15     6     9    0   60.0%  <- STOP
culprit commits: c9f09dd x7, 00d17cb x2   (both this chain's own fix commits)
```

Regressions outnumbered originals. Nine of fifteen findings cite lines this chain itself added —
the loop reviewing its own fixes, which is invisible from inside because every finding is real.
**No round 4 was dispatched, and none should be.** Per § Rounds the response is *revert to the root,
not a third patch*, and that is what the fixes below do: findings 1, 3, 4 and 8 are all resolved by
**deleting** a contortion rather than adding a layer to it.

| # | disposition | how |
|---|---|---|
| 1 | **fixed — reverted to root** | Two rounds of arithmetic (`floor(n/2)`, then `min(n, max(2, floor(n/2)))`) each broke at an edge, which is the signal that the *reading* was wrong: the retry budget was never a function of `n`. It is now a named constant, `CAPPED_RETRY_ITERATIONS = 2` — one fix-and-re-verify cycle. Capped implement cost is exactly `max_iterations + 2` vs uncapped `1 + max_iterations`, at every n, no edges. |
| 2 | **fixed** | The dangerous mutation — dropping `implement()`'s `moved ?` guard, halving a *genuine* escalation's budget — passed all 184 assertions. Scenario B now asserts an escalated retry keeps the FULL budget. Scenario G (new, n=8) pins the constant: at n=3 and n=4 a halving also yields 2, so E and F alone could not tell a constant from an arithmetic function. |
| 3 | **fixed — claim deleted, not rewritten** | The round-2 comment asserted a false safety property; its replacement asserted a *different* false one (`plannerPrompt` renders a complete, wrongly-framed prompt with `st` undefined — only the implementer builders throw). A third assertion was not written. The comment now states the convention, states plainly that it is **not** a structural guarantee, and names the cap-sweep assertions as what actually catches a forgotten `st`. |
| 4 | **fixed** | The superseded comment block was left sitting above its replacement — two consecutive paragraphs opening with the same sentence and reaching opposite conclusions. Deleted. |
| 5 | **fixed** | ADR 0016 § 2 amended: it still taught the event-flag rule the code now contradicts, so a maintainer resolving a disagreement via the doc hierarchy would have restored the bug. The amendment states the terminality rule, says explicitly not to restore the event-flag reading, names Scenario F as the guard, and documents the retry constant as part of the consequence. |
| 6 | **fixed** | "~1.5 ×" was only true for n≥4 — 1.67x at this template's own default of 3, and 2.0x at n≤2. Now the exact `max_iterations + 2`. |
| 7 | **fixed** | `execute/SKILL.md:83` still advertised the `(opus \| fable)` menu the template had been corrected away from. It is the agent-facing surface that resolves frontmatter into args, so the disagreement mattered; it now names `opus` as the only capping value and flags the empty-value fail-open. |
| 8 | **fixed — reverted to root** | `effortTier` is back to `terminalTier(st) ? TOP_TIER : st.tier`. `&& st.tier !== TOP_TIER` was dead logic and `st.capSuppressed ||` was production-unreachable, kept alive only by two doubles that omitted `cap` — a state `converge()` cannot produce. The doubles were given a `cap` rather than the engine being shaped to satisfy them. |
| 9 | **fixed** | The prose above the helper assertions still taught "effort changes when suppression happens" — the rule Scenario F disproves — in the position a reader hits first. Rewritten to the terminality contract. |
| 10 | **fixed** | The round-2 correction block published the *pre-fix* formula as the corrected record. Rewritten, and explicitly marked as superseded twice so a reader takes only the final line. |
| 11 | **fixed** | Scenario F now asserts `tierCappedAt === 'implement'` (the structural fact Scenario E cannot see, since E has a plan layer) and that the ceiling held on every dispatch of the non-plan-gated path. |
| 12 | **deferred** → THREAD.md § Open questions | Real and pre-existing: `/thread:status` and `/thread:repair` never mention `tier_capped`, though ADR 0016 § 3 names them as its consumers. Out of range, and fixing it means editing two conductor skills — not a fourth patch on a chain the ledger has stopped. |
| 13 | **fixed** | The THREAD.md correction recorded the loss of the cache barrier as pure upside. It now records the hazard: with a directory source, uncommitted half-finished edits are live estate-wide, with the behavioural guard and a two-place release verification. |
| 14 | **deferred** → THREAD.md § Open questions | Real and pre-existing: a capped run that goes green on its first pass never calls `escalate()`, so `tierCapped` is false and nothing records that it ran ceilinged. Fixing it means a new result field plus a reconcile change — a design decision, not a cleanup. |
| 15 | **fixed** | The cost sentence modelled `implement()` only, while `reviserPrompt` renders a full `ralphLoop(task.maxIterations)` per review round. The template now gives the whole per-task figure. |

### Verification

- Full README § Tests suite green (7/7). `prompt-invariants.test.mjs`: **191 assertions, 0 failures**
  (was 184).
- Mutations, all now caught — the first is the one that passed the entire suite before:

  | mutation | caught by |
  |---|---|
  | drop `moved ?` (halves a REAL escalation) | `effort B: a REAL escalation keeps the FULL max_iterations` |
  | `CAPPED_RETRY_ITERATIONS = 4` (revert toward halving) | cap E, cap F, cap E+F, cap G |
  | restore the event-flag effort rule | 3 assertions incl. cap E and cap F |
  | remove the judge clamp | 3 assertions incl. cap E |
