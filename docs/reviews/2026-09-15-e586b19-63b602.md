---
repo: /Users/lachlants/repos/tools/thread-skill
head: e586b19
diff_digest: 63b602
harness: claude
engine: code-review
mode: code
effort: high
reviewed: 2026-09-15
status: pending
---

# Clean-room code review — thread-skill @ e586b19 (working tree)

Scope: working-tree diff (8 modified files + untracked `docs/adr/0015-process-candidates-route-by-altitude.md`),
cross-checked against `~/.agents/skills/method/SKILL.md`,
`~/repos/workspaces/_shared/docs/adr/0003-method-at-every-altitude.md`, the live METHOD.md ledgers on disk,
and `skills/_shared/task-writer.md`.

| # | file:line | summary | failure scenario | verdict | category |
|---|---|---|---|---|---|
| 1 | skills/stash/SKILL.md:18 (and skills/defer/SKILL.md:18) | Give stash/defer a way to resolve a project METHOD.md path before removing the "project directory resolved" guard | `/thread:stash` from `~/repos/animately/giflab` with a project-shaped observation: routing step 1 says "the project's METHOD.md", but stash/defer only resolve a vault project *note* via `task-writer.md` § 1 and never run close's "CWD inside `~/Projects/<Area>/<Project>/`" branch, so the agent guesses a path, drops a stray METHOD.md in the code repo root, or over-routes to the estate — where the old text NOOPed safely | HIGH | — |
| 2 | skills/close/SKILL.md:38 | Restore sub-seats to the restated altitude routing test | Closing an Animately SEO project with a seat-shaped row writes to `animately-workspace/knowledge/METHOD.md`, which does not exist, creating a second ledger beside the live `animately-workspace/seo/knowledge/METHOD.md` and splitting the seat surface the 2026-09-15 migration consolidated; the same restatement drops the canonical test's `_shared/knowledge/projects-schema.md` pointer while claiming to be "cited here, never restated" | MEDIUM | — |
| 3 | skills/close/SKILL.md:38 and docs/adr/0015-process-candidates-route-by-altitude.md:44 | Define a seat-ledger fallback for a CWD under no workspace | A seat-shaped row captured while closing in `~/repos/tools/thread-skill` resolves no "CWD workspace" seat ledger and cannot fall back to step 1's project ledger in a no-project context, so the row falls on the floor — the exact motivating case in ADR 0015's own Context; workspaces ADR 0003 line 60 shares the gap | MEDIUM | — |
| 4 | skills/stash/SKILL.md:18, skills/defer/SKILL.md:18 | Carry close's commit-hygiene rules into stash/defer now that auto-commit reaches `~/repos/workspaces` | An estate-shaped row from `/thread:stash` appends to `_shared/knowledge/METHOD.md` and a bare `git -C ~/repos/workspaces commit` sweeps in whatever a concurrent session has staged in that shared config repo — ADR 0014 scoped the auto-commit to the `~/Projects` monorepo, and this diff widens the target without widening the guard (`skills/close/SKILL.md:134-144` holds the rules neither skill cites) | MEDIUM | — |
| 5 | docs/windows-setup.md:17 | Correct the Windows NOOP claim, which the same page's own clone instruction falsifies | A Windows user follows line 15 and clones `lachyts/claude-workspaces` to `C:\Users\<you>\repos\workspaces`; the workspace `knowledge/` and `_shared/knowledge/` surfaces now exist, the routing test routes to them, and the documented "none of those surfaces exist there" guarantee is false — and the paragraph still names only `close`, giving a Windows `/thread:stash` no guidance despite ADR 0014/0015 binding it to the same scan | MEDIUM | — |
| 6 | .claude-plugin/plugin.json:3 (and .claude-plugin/marketplace.json:12) | Bump the plugin version for a behaviour-changing routing edit | Both manifests stay at `2.3.4` (released by the already-committed e586b19) while close/stash/defer routing changes and ADR 0015 lands, so installs running `claude plugin marketplace update thread` see no version change and may not pick up the new skill bodies — against the repo's one-bump-per-behaviour-change convention (2.3.0/0012, 2.3.1/0013, 2.3.2/0014, 2.3.3–2.3.4 orient+rollout) | MEDIUM | — |
| 7 | docs/adr/0015-process-candidates-route-by-altitude.md:53-55 | Stop listing the row form and `K` prefix as "Unchanged" when this change set introduces them here | A reader trusting the ADR's Unchanged list believes row form is stable, but `skills/close/SKILL.md:54` moves from `- YYYY-MM-DD [provisional] <observation> — … evidence: <links>` to `… [provisional] K<nn> — <observation> — … evidence: <path § heading>`; they are unchanged in the *method* contract, not in this repo's writer — material because ADR 0012's cross-repo lockstep rule makes row-form changes the thing that must move together | LOW | — |
| 8 | CONTEXT.md:91 | Define or point at "seat" and "estate" in the glossary, and stop attributing category 7 to close alone | A reader hits "seat" and "estate" as load-bearing terms with no entry and no pointer to `~/repos/workspaces/_shared/CONTEXT.md` or `_shared/docs/adr/0003`; the same bullets still read "close's scan category 7" / "close appends process observations to" although ADR 0014 added stash/defer and ADR 0015's Consequences says the lockstep binds all three writers | LOW | — |
| 9 | CONTEXT.md:96-98 | Add `[correction]` to the candidate status vocabulary | The glossary lists five tokens and claims authority on the distinction from the memory vocabulary, but `method/SKILL.md` § Candidates defines six; a reader who hits a `[correction]` row — the mechanism for fixing an immutable row — finds no entry | LOW | — |
| 10 | skills/close/SKILL.md:128 | Resolve the contradictory commit instruction for seat/estate ledgers | An agent reading top-down executes "auto-commit that file in its containing repo … versioned immediately rather than waiting on the daily sweep", then three steps later reads that a seat/estate ledger should instead be named at sub-step 6 "rather than committing it twice" — by which point the first imperative has already run, and the stated rationale no longer holds for those rows | LOW | — |
| 11 | skills/close/SKILL.md:146 | Widen the "What landed" rationale beyond project ledgers | The justification now reads "*a project ledger write* lands outside the workspace and vault, so it is never silent", so an agent can reasonably omit seat/estate appends (which land inside the auto-committed workspaces repo) from the report — reintroducing silence for exactly the two destinations this change adds | LOW | — |

## Notes on this run

- The engine emitted **severity labels** (HIGH / MEDIUM / LOW), not `CONFIRMED` / `PLAUSIBLE` verdicts and not
  its usual category taxonomy — `ReportFindings` was absent from its registry, so it restated findings as prose
  rather than through the structured channel. The `verdict` column carries the severity it actually gave;
  `category` is left as `—` rather than invented.
- `effort: high` in the front matter is the level requested and the level the engine ran at. However, the engine
  **degraded to a single inline pass**: it forked itself to a depth-2 agent which had no `Agent` tool, so the
  multi-agent angle fan-out did not run. Treat coverage as one reviewer at high effort, not the full fan-out.
- `README.md`, `docs/adr/0012-process-candidates-flow-to-method-md.md` and
  `docs/adr/0014-stash-and-defer-run-the-silent-process-scan.md` were in scope and produced no findings.
- Finding 6 cites `.claude-plugin/plugin.json` / `.claude-plugin/marketplace.json`, which are inside this repo
  but outside the nine files named as the review target. Kept as the engine produced it, flagged here.
