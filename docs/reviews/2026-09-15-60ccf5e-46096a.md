---
repo: /Users/lachlants/repos/tools/thread-skill
head: 60ccf5e
diff_digest: 46096a
harness: claude
engine: code-review
mode: code
effort: high
reviewed: 2026-09-15
status: pending
---

# Round 4 clean-room review — ADR 0015 altitude routing

Scope: the 11 named files, all uncommitted against `60ccf5e`
(`.claude-plugin/marketplace.json`, `.claude-plugin/plugin.json`, `CONTEXT.md`, `README.md`,
`docs/adr/0012-…`, `docs/adr/0014-…`, `docs/adr/0015-…`, `docs/windows-setup.md`,
`skills/close/SKILL.md`, `skills/defer/SKILL.md`, `skills/stash/SKILL.md`).

## Findings

| # | file:line | summary | failure scenario | verdict | category |
|---|---|---|---|---|---|
| 1 | skills/close/SKILL.md:33 | Fix the project-directory resolver — it cannot resolve the case it exists for | Closing from `~/repos/animately/giflab`, the paired note `Work/Projects/Animately/GifLab.md` holds `repos: - ~/repos/animately/giflab` and `` Local: `~/repos/animately/giflab/` `` — both the code repo, never a `~/Projects/` path — so no project directory resolves, the routing test skips step 1, and a giflab-only arrangement observation over-routes to seat/estate while the existing `~/Projects/Animately/giflab/METHOD.md` is orphaned. | HIGH | correctness |
| 2 | skills/close/SKILL.md:35 | Reconcile "Nothing else resolves" with the method skill's altitude resolver it defers to | Running `/thread:close` inside `~/repos/tools/thread-skill` (which has a `THREAD.md`) routes every observation to the estate per ADR 0015, while a later `/method` in the same cwd resolves *project* altitude off that same `THREAD.md` and creates `~/repos/tools/thread-skill/METHOD.md` — a ledger ADR 0015 forbids, holding rows about work already sent to the estate. | MEDIUM | correctness |
| 3 | skills/close/SKILL.md:47 | Give the "unsure → project" tie-break a destination in the no-project case | An agent genuinely undecided between seat and estate, closing from any repo where no project directory resolves (the common path given finding 1), finds the tie-break points at "project" — which the next sentence makes unavailable — and the method skill's no-project branch offers only seat/estate with no unsure fallback, so it guesses, in the direction ADR 0015 says nothing corrects. | MEDIUM | correctness |
| 4 | CONTEXT.md:109 | Drop the retired apply gate the same diff supersedes | An agent resolving the `## Method` write rule through the repo's designated term authority reads "the gate-protected `## Method` section belongs to `/method` apply" and stops to request approval for routine curation that `skills/close/SKILL.md:63` and the method skill's Working agreement now authorise automatically. | MEDIUM | correctness |
| 5 | skills/close/SKILL.md:153 | Stop the commit incantation failing silently, and order it after the pre-scan it depends on | A project ledger written under `~/Projects/Tutorials/`, `/_archive/` or `/TSMS/` hits `git check-ignore -v Tutorials/METHOD.md` → `.gitignore:35:/Tutorials/`, so `git add` exits non-zero, `&&` short-circuits, step 7.3 has no failure branch, and step 8 reports a commit SHA for a commit that never happened; separately the block sits above the `diff --cached --name-only` pre-scan at :159, so an agent reading in order stages its own file first and can no longer distinguish it from the foreign pre-staged work that check exists to catch. | LOW | correctness |
| 6 | skills/stash/SKILL.md:18, skills/defer/SKILL.md:18 | Cite close's Commit hygiene instead of inlining a divergent copy of it | Both files assert there is "no second definition … here", then paste close's `add`-then-pathspec command verbatim while omitting its `--cached` pre-scan — already divergent on first write, so the next change to close's Commit hygiene leaves stash and defer silently running the old contract. | LOW | cleanup / duplication |

## Verified clean

- Version bump `2.3.5` consistent across `plugin.json` and `marketplace.json`.
- Every new cross-reference resolves: method § `Which ledger a row goes to`, `_shared/CONTEXT.md` § Method,
  workspaces ADR `0003-method-at-every-altitude.md`, `METHOD-template.md`, README's ADR 0013/0014/0015 titles.
- ADR 0015's claim that `knowledge/processes.md` no longer exists is true (`find` returns nothing).
- The sub-seat example path `animately-workspace/seo/knowledge/METHOD.md` exists.
- No seat ledger yet holds a `K` row, so close's "`K01` when none" is correct.
- Close's row form matches the method contract's `<ID>` / `path § heading` shape.
- The step 7.3 → 7.6 ordering claim is accurate.
- The Windows clause is verbatim-identical across close, stash and defer, as `docs/windows-setup.md:17` asserts.

Round-3 fixes that did land: Windows clause moved into the skill bodies, sub-seat path named,
`K` ordinal disambiguated, row form matched to the method contract, 0014 amendment notes added,
`[correction]` token present on both sides of the glossary.

**Bottom line:** finding 1 is the blocker — the fix added this round does not reach the case it was
written for, and the existing `giflab` and `compression-engine` project ledgers go orphaned the first
time anyone closes from those repos.

## Provenance

- Engine: Claude Code built-in `/code-review`, invoked at `high` on the 11-file target.
- `ReportFindings` is absent inside a subagent, so the engine forked itself and reported inline.
- The forked engine agent made no `Agent` calls: the multi-agent fan-out **did not run** — this was a
  single-pass review at `high`. The `effort: high` front-matter value above is the level requested and
  the level the single pass ran at; no per-angle agents were dispatched.
- Clean-room caveat: the engine read `docs/reviews/` (the round-3 doc) of its own accord at step 17 of
  its investigation, despite the dispatch instruction to this lead not to. The findings above are
  therefore not fully round-3-blind; the known `/code-review` behaviour of cat-ing prior review docs
  applies.
