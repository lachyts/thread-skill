---
repo: /Users/lachlants/repos/tools/thread-skill/.claude/worktrees/enabler
head: c5f7421
diff_digest: 399e2e
harness: claude
engine: simplify
mode: simplify
effort: n/a
reviewed: 2026-09-23
status: consumed
---

| # | file:line | summary | failure scenario | verdict | category |
|---|---|---|---|---|---|
| 1 | skills/execute/SKILL.md:175 (+ skills/execute/scripts/merge-wave.sh:417) | The "one source" default branch is resolved in two places: the SKILL.md prose block asks `git ls-remote --symref`, `merge-wave.sh` asks `gh repo view --json defaultBranchRef`, and the engine also falls back to `'main'` | The two use different transports with different auth and failure modes, so they can disagree. The resolver runs only when the model copies the prose and fills in `<repoPath>` by hand. `tests/default-branch.test.sh:14-15` pulls it out with awk/sed, so an ordinary edit to the prose breaks the test. Simpler form: `skills/execute/scripts/default-branch.sh <repoPath>`. §4 calls it via `${CLAUDE_PLUGIN_ROOT}` (as it already calls `reconcile-wave.py` and `merge-wave.sh`), the test calls the script directly (drop the awk/sed), and `merge-wave.sh` may set `BASE` from it | apply (the script extraction; `merge-wave.sh` may keep its `gh` read if its fake-`gh` test depends on it) | altitude / reuse |
| 2 | skills/execute/SKILL.md:220 (+ :51, :191, :412, :435; skills/status/SKILL.md:68; skills/execute/scripts/merge-wave.sh:14) | The main-to-base prose sweep is partial, and each edited spot rephrases "`main` unless §4's `defaultBranch`" | `:220` (the smart-halt step an operator acts on) still says "freshly-merged `origin/main`". `:435` and `merge-wave.sh:14` still say `main`. `status:68` cites `origin/<defaultBranch>`, which status cannot know because the value is a per-wave arg that is never stored. Simpler form: define "the base branch" once in §4, write "the base branch (§4)" or `origin/<base>` everywhere else, and fix :220, :435 and :14. `skills/schedule/rollout-template.md:32,81` also say `main` (outside the diff) | apply (in-diff files); rollout-template.md noted, outside the diff | altitude / simplification |
| 3 | skills/execute/SKILL.md:414 | The self-rollout hold branch is a manual convention (`git switch -c hold/<version>`) that relies on a side effect of `refresh_local_base`'s skip | If someone forgets the switch, live sessions load half-rolled-out skill text. With the root parked, the planner and investigator prompts read `${a.repoPath}` directly (`wave-execute.workflow.js:480,502`) and see the pre-rollout tree, so the "cosmetic" label (`merge-wave.sh:86`) is wrong for those readers. Deeper fix: load the installed plugin from a release worktree or tag, or make the hold a declared rollout-note field that is passed to `merge-wave.sh` | skip: needs changes well outside the diff; the parked-root planner point is a correctness question for /code-review | altitude |
| 4 | skills/execute/scripts/merge-wave.sh:114 | `--self-test-base` (~28 test lines) ships inside the production script. It forces the "defined before the self-test hooks" ordering rule (:91-92) and repeats `case "$out" in *X*) sb_ok y y L;; *) sb_ok n y L;; esac` three times (:130, :136, :138) | The same contract is tested in two places (`tests/merge-wave-base.test.sh` is its stated home). Simpler form: in `merge-wave-base.test.sh`, `eval "$(sed -n '/^refresh_local_base() {/,/^}/p' skills/execute/scripts/merge-wave.sh)"` and assert with the existing `has()`. Then drop the `tests/run.sh:58` step and the ordering note. Alternative: make the script sourceable by guarding the main body with `[[ "${BASH_SOURCE[0]}" == "$0" ]]` | apply | altitude / simplification |
| 5 | skills/execute/scripts/merge-wave.sh:95 | `refresh_local_base`'s empty-base branch (:95-98, plus the `('' ⇒ unresolved)` note on :93) can never run. Its only caller (:449) comes after :418, which exits when `BASE` is empty | Dead code, kept alive only by its self-test case (:137-138). Simpler form: delete :95-98, the note on :93, and the "unresolved base" self-test case | apply | simplification |
| 6 | tests/lib/engine.mjs:12 | `loadEngine()` is a second copy of the vm loader in `skills/execute/tests/prompt-invariants.test.mjs:31-43`: the same orchestration marker, `meta` rewrite, `__t` export and `log`/`agent` stubs, and its header comment says so. `tests/default-branch.test.mjs:43` `st` also duplicates `ST_OPUS` (prompt-invariants :68) | A change to the marker or the stubbed globals has to be made twice. Simpler form: `export function loadEngine(names, ctx = { console, log: () => {}, agent: async () => null })`. prompt-invariants imports it and passes its own `ctx` (it swaps `ctx.agent` per case), and `ST_OPUS` is exported from `tests/lib/engine.mjs` | apply (touches the adjacent prompt-invariants.test.mjs; may conflict with the protocol 4 branch) | reuse |
| 7 | skills/execute/SKILL.md:183 (+ :140-142) | The rule "pass `defaultBranch` only when it is not `main`" repeats what the engine already does: `'main'`, `''` and unset all render byte-identical prompts (pinned by `tests/default-branch.test.mjs:28-31,53-55`) | The conductor makes an extra comparison every wave. An absent arg could mean "resolved to main", "a legacy run" or "the resolver was skipped", so the "never assume main" rule can't be seen in a run's args. Simpler form: always pass the resolved name on new runs and keep the byte identity in the engine; `\|\| 'main'` then serves only legacy resumes | apply | altitude |
| 8 | Makefile:13 | `release-check` starts python twice to re-implement the manifest version-agreement check that `tests/contracts/manifest.test.mjs:53` already runs. A separate `[ -d ]` loop also duplicates what `diff -rq` reports | The version rule lives in two places, and the recipe is six chained lines calling both python and node. Simpler form: `@node --test --test-name-pattern='same version' tests/contracts/manifest.test.mjs >/dev/null \|\| {…}`, then `v=$$(node -p 'require("./.claude-plugin/plugin.json").version')` and a single `for sub in skills hooks; do diff -rq -x .DS_Store -x __pycache__ "$(CACHE)/$$v/$$sub" $$sub \|\| r=1; done` (tested in scratch against a missing, a matching and a differing cache) | apply | reuse / simplification |
| 9 | tests/run.sh:57-62 | Six independent shell suites run one after another: the two merge-wave self-tests, reconcile-wave, wave-stop-driver, default-branch and merge-wave-base | Measured: about 5.9s in series against 2.85s in parallel. This is the self-rollout verifier, which runs on every implementer iteration. Cheaper alternative: start each suite as a background job writing `$scratch/$i.out` and `$scratch/$i.rc`, `wait`, then print outputs and record failures in the original order (bash 3.2-safe, about 10 lines) | apply | efficiency |
| 10 | tests/run.sh:32 | `sleep 1` after writing the stamp is wasted on APFS and ext4, where `find -newer` compares mtimes to the nanosecond | A flat 1.0s, about 12% of the 8.1s run. Cheaper alternative: sleep only on filesystems that store whole-second mtimes: `python3 -c 'import os,sys; sys.exit(os.stat(sys.argv[1]).st_mtime_ns % 10**9 == 0)' "$stamp" \|\| sleep 1` | apply | efficiency |
| 11 | skills/execute/wave-execute.workflow.js:723 | `BRANCH_NAME` stacks eight lookaheads that encode the rules the comment at :719-721 lists in prose | You can't check the regex against those rules by reading it, and adding a rule is risky. Simpler form, one clause per rule (identical to the current regex on all 1,111,110 strings of up to 6 characters over `ab.-/_l0ck`): `typeof b === 'string' && /^[A-Za-z0-9._\/-]+$/.test(b) && !b.startsWith('-') && !b.endsWith('.') && !b.includes('..') && b.split('/').every((c) => c && !c.startsWith('.') && !c.endsWith('.lock'))` | apply | simplification |
| 12 | skills/execute/scripts/merge-wave.sh:419 | The `NUMS`/`STATES` parallel arrays are kept aligned by a manual `i=0 … i=$((i+1))` counter (:440), and a temporary `sb` is split in two separate expansions | Two arrays aligned by position, plus a counter. Simpler form: one array of pairs: `read -r st b <<<"$(gh pr view …)"`; `WAVE+=("$st $PR")`; then `for e in "${WAVE[@]}"; do st=${e%% *}; PR=${e#* }; …`. The array is never empty, so bash 3.2 `set -u` is safe | apply | simplification |
| 13 | skills/execute/scripts/merge-wave.sh:349 | The branch-delete guard is four chained tests, and the `main`/`master` literals are leftovers now that `$BASE` is verified | Hard to read at a glance. Simpler form with the same behaviour: `case "$br" in ''\|"$BASE"\|main\|master) ;; *) <delete block> ;; esac`. Deeper fix: delete only the engine's own namespace (`audit-fix/*`, `wave-execute.workflow.js:736`) | apply the case form; skip the audit-fix/* restriction, which changes which branches get deleted | simplification / altitude |
| 14 | tests/default-branch.test.sh:8 (+ :59, :63; tests/merge-wave-base.test.sh:10-11) | `ok()` is byte-identical in both shell test files. `has()` is defined in merge-wave-base but written out inline in default-branch.test.sh at :59, and as `[ -d ] && ok y y L \|\| ok n y L` at :63 | The copies drift, and each label is typed twice. Simpler form: `tests/lib/assert.sh` with `fail=0`, `ok()` and `has()`, sourced by both files. :59 becomes `has "$setup" 'origin/master && cd "$WT"; fi' "…"`; :63 becomes `ok "$([ -d "$wt" ] && echo y)" y "…"` | apply | reuse / simplification |
| 15 | tests/contracts/manifest.test.mjs:96 | The `${CLAUDE_PLUGIN_ROOT}` reference check scans a hand-picked list (`skills/_shared`, `skills/schedule`, `skills/execute`), then removes the duplicates with a `Set` | A new `.md` in any other skill directory silently escapes the check. Simpler form: ``const files = ['README.md', ...fs.readdirSync(path.join(root, 'skills'), { recursive: true }).filter((f) => f.endsWith('.md')).map((f) => `skills/${f}`)]`` and drop the `Set` (it covers the same 17 files today) | apply | simplification |
| 16 | skills/execute/diagnostics/edit-noop-repro.workflow.js:72 | The diagnostic says it "mirrors the wave-execute engine verbatim" (:11, :68) but hardcodes `origin/main`, which the engine no longer does after this diff | The claim is now false, and the diagnostic fails on a scratch repo that has only `master`. Simpler form: take the base from args (`a.defaultBranch \|\| 'main'`) or drop the "verbatim" claim. The file is a pure move (R100 from skills/execute/tests/), so the hardcode predates the diff | apply (low) | altitude |
| 17 | skills/execute/scripts/merge-wave.sh:121 (+ tests/default-branch.test.sh:20-24) | The same `master`-origin git fixture is built twice: the `g()` wrapper, a bare master repo, a seeded clone, and a second clone pushing a "wave" commit | Duplicate fixture code. Simpler form: a `mk_master_origin` helper in the `tests/lib/` shell helper | apply only together with #4 (the shipped script cannot source tests/lib) | reuse |
| 18 | skills/execute/scripts/merge-wave.sh:423 | An inline `gh pr view … --json state,baseRefName` sits next to the existing `prfield()` (:206) with a different failure rule (empty string instead of a non-zero exit), and the fake `gh` (`tests/merge-wave-base.test.sh:27`) needs a special branch just for it | A second way to read a PR. Suggested form: `st=$(prfield "$PR" state)`; `b=$(prfield "$PR" baseRefName)`, and drop the fake `gh`'s special branch | skip: doubles the per-PR API reads, against the stated "one read per PR" design | reuse |
| 19 | skills/execute/scripts/merge-wave.sh:144 | The comment "this script otherwise has no unit test" is out of date now that `tests/merge-wave-base.test.sh` runs the script end to end with a fake `gh` | Misleading comment. Simpler form: point it at `tests/merge-wave-base.test.sh` | apply | simplification |

## Disposition (consumed 2026-09-23)

Engine-file items are held for protocol 4 (Lachy's ruling, 2026-09-23: branch
`codex/thread-rollout-redesign` rewrites those files). Everything else was applied in task
thread-skill-p1-1, or skipped as already ruled.

| # | disposition |
|---|---|
| 1 | held for protocol 4 (skills/execute/SKILL.md § 4; the resolver moves to a new skills/execute/scripts/default-branch.sh wired from there) |
| 2 | held for protocol 4 (skills/execute/SKILL.md § 4 and § 4.5 step 4, skills/execute/scripts/merge-wave.sh:14). Its non-engine spots (status/SKILL.md:68, execute SKILL.md :51/:412/:435, schedule/rollout-template.md:32,81) hang on that § 4 definition. |
| 3 | skip (answered by the rollout clone in 2.5.3) |
| 4 | held for protocol 4 (skills/execute/scripts/merge-wave.sh `--self-test-base`, with its tests/run.sh step) |
| 5 | held for protocol 4 (skills/execute/scripts/merge-wave.sh) |
| 6 | held for protocol 4 (skills/execute/tests/prompt-invariants.test.mjs) |
| 7 | held for protocol 4 (skills/execute/SKILL.md § 4) |
| 8 | applied (release-check reuses the manifest contract test and reads the version with node; one cache-dir guard, one diff loop) |
| 9 | applied (tests/run.sh runs shell suites and merge-wave self-tests concurrently; output grouped in the original order) |
| 10 | applied (the stamp sleep runs only when the stamp's mtime lands on a whole second) |
| 11 | held for protocol 4 (skills/execute/wave-execute.workflow.js) |
| 12 | held for protocol 4 (skills/execute/scripts/merge-wave.sh) |
| 13 | held for protocol 4 (skills/execute/scripts/merge-wave.sh) |
| 14 | applied (tests/lib/assert.sh; both suites source it; the inline case and `[ -d ] && ok \|\| ok` pair become has/ok) |
| 15 | applied (the manifest reference scan is recursive over skills/**/*.md) |
| 16 | applied (the diagnostic takes `origin/${defaultBranch\|\|main}`; default-branch.test.sh asserts it has no origin/main) |
| 17 | held for protocol 4 (skills/execute/scripts/merge-wave.sh; only together with #4, because the shipped script cannot source tests/lib) |
| 18 | skip (doubles per-PR API reads against the "one read per PR" design) |
| 19 | held for protocol 4 (skills/execute/scripts/merge-wave.sh) |
