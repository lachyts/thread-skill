---
repo: /Users/lachlants/repos/tools/thread-skill
head: d2b36d6
diff_digest: b59fd7
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-26
status: pending
---

Target: `73bcdd0...ci/review-round-1` (PR #22, round-1 fixes). The cell is `o48-xhigh-v1` (no verify phase, ungraded). It returned exactly the 15-finding cap.

| # | file:line | summary | failure scenario |
|---|---|---|---|
| 1 | docs/adr/0024-the-operators-top-tier-sets-every-rollouts-ceiling.md:30 | "The lower of stamp and file" means raising the top tier never lifts an already-stamped rollout, which contradicts "changes every future dispatch in one place"; round-1 #8 is not fixed | Stamped opus, file later set to fable or removed: min is still opus, so every paused or resumed rollout stays capped until each note is hand-edited. |
| 2 | docs/adr/0024-…ceiling.md:30 | Re-reading the live file on resume breaks execute's resume contract (resumeFromRunId must re-pass the original args, execute/SKILL.md:183) | The file changes between runs, the resume passes a different maxTier, and the model, effort and verify shape change, so the cached replay diverges. |
| 3 | skills/execute/SKILL.md:294 | Round-1 #1 is fixed only in the ADR and CONTEXT; the live skill still says a tierCapped block is "not a wall… re-dispatchable uncapped once quota returns" | Under today's operator lock, a real wall gets planned for an uncapped fable re-dispatch the lock forbids. |
| 4 | docs/adr/0024-…ceiling.md:38 | "Wall or wait by kind of cap" has no classifier: the engine gets a bare maxTier, and a file-derived stamp looks the same as a quota stamp | After the file is removed, only the stamp binds and a wall is read as "wait" again; both sources set gives an undefined kind. |
| 5 | docs/adr/0025-master-moves-only-by-green-pr-owner-included.md:37 | The lift/restore and rename procedure covers only the ruleset; classic protection still requires the same contexts | A job rename or an emergency disable leaves classic protection blocking every PR. The "payload" is prose only. |
| 6 | docs/adr/0024-…ceiling.md:38 | "Real walls (ADR 0006)" contradicts 0016 consequence 3: a capped block can be a capacity block | A task fable would solve is triaged as a wall, and the fact that raising the top tier would fix it is lost. |
| 7 | docs/adr/0025-…owner-included.md:21 | The "undetermined" push 4c285d2..73bcdd0 was PR #21's head (mergeCommit == headRefOid), which looks like a fast-forward of a green PR head rather than a bypass, so "classic is weaker" may rest on non-equivalent tests | Future decisions rely on a misdiagnosed leak. |
| 8 | README.md:124 | The release step `git switch master && git pull --ff-only` fails while commits sit stranded on local master (the state ADR 0025 describes) | The merge fallback makes a local merge commit, and the tag lands off origin/master, which is the orphan-tag bug again. |
| 9 | CONTEXT.md:261 | The Ceiling entry states the ADR 0024 wall rule in the present tense, with no implementation-pending note, and contradicts execute/SKILL.md:294 | The glossary and the skill disagree about live behaviour. |
| 10 | CONTEXT.md:252 | "Never the ladder's own top rung (fable)" is false when the top tier is fable | A reader concludes fable isn't a valid top-tier value. |
| 11 | .github/workflows/test.yml:20 | The workflow-level concurrency group still cancels a pending master-push run even with cancel-in-progress false | Three quick merges: the middle master commit is never tested. |
| 12 | .github/workflows/test.yml:19 | All runs for a PR share one group, so re-running an older run cancels the head's required checks | The required contexts end up "cancelled", the PR is BLOCKED, and merge-wave halts. |
| 13 | tests/repair-stranded-merged.test.sh:117 | region() still passes patterns through `awk -v`, so a future `\\.` is loosened again on Linux; the ENVIRON form in handoff-home.test.sh:91 avoids it | A pattern that passes on macOS is a wildcard on Ubuntu, which only warns. |
| 14 | docs/reviews/2026-09-26-56679f6-ad8453.md:10 | Marked consumed ("all 15 fixed or recorded"), but #14 (THREAD.md:431) is neither | The next session re-plans p5-1. |
| 15 | docs/adr/0024-…ceiling.md:1 | 01a2e97 bundles unrelated doc changes, and 316510f mixes a test fix into chore(ci) | A revert of one takes the others with it (AGENTS.md: split unrelated changes). |
