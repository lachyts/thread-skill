---
repo: /Users/lachlants/repos/tools/thread-skill
head: 07ddc75
diff_digest: e55ab3
harness: claude
engine: code-review
mode: code
effort: xhigh
reviewed: 2026-09-21
status: pending
---

# Review — round-2 consumption batch (tier-ceiling fixes for findings 6–12)

Clean-room `xhigh` pass over the uncommitted 4-file batch at `07ddc75` plus `THREAD.md`.
This is a **re-dispatch**: the first round on `7ed7e6f`/`aaa87d` was killed mid-run when the
Claude Code process exited, produced no findings and no doc. Scope was re-derived at the new
HEAD rather than reused.

The engine forked into a background `code-review` agent but returned its result synchronously
through the wrapper's Skill call, so the wrapper held the findings and relayed them. Findings
verified by the engine with sandbox probes driving `converge()` plus mutation tests against the
suite; the wrapper confirmed the working tree was restored (same 4 modified files, HEAD still
`07ddc75`).

## Findings

| # | file:line | summary | failure scenario | verdict | category |
|---|---|---|---|---|---|
| 1 | `skills/schedule/rollout-template.md:24` | Template comment promises "a capped tier is terminal, so it runs the FULL loop at the higher tier's effort", but the capped first pass runs the full Ralph loop at the LOWER tier's effort row — `effortTier()` keys off the lagging `st.capSuppressed` event flag instead of the run-level fact that the run is capped. | Probe-confirmed at HEAD: `converge({slug,scope:'single-file',planGate:false,maxIterations:3},{maxTier:'opus'})` dispatches `implement:cap-default model=opus effort=medium maxIters=3 oneShot=false` — `EFFORT.opus.implementer` ('medium'), not `EFFORT.fable.implementer` ('high'). Template default is `plan_approval: scope-gated`, so every `scope: single-file` task takes this path; the common capped task never gets the promised effort row. Scenario E masks it (uses `planGate:true`, so the plan layer sets `capSuppressed` before `implement()`), and test:333 asserts 'high' under a label reading as the general case. Fix: derive effort from `terminalTier(st)`/`st.cap` (a run constant), not from whether an escalation happens to be suppressed yet. | Bug — behaviour contradicts documented contract | Correctness |
| 2 | `skills/execute/wave-execute.workflow.js:1042` | At the template default `max_iterations: 3`, the budget-split halving gives the capped retry exactly ONE iteration — a single verifier run with zero verified fix iterations, the degradation ADR 0016 §1 says the cap must not deliver. | Probe-confirmed: `max_iterations: 3` + `maxTier:'opus'` ⇒ `retryTask.maxIterations = Math.max(1, Math.floor(3/2)) = 1`, prompt renders `Max iterations: 1`. `ralphLoop` at 1 iteration runs the verifier once, applies a fix at step (c), hits step (d) and blocks without re-running the verifier — agent commits an unverified fix and reports blocked. The higher-effort pass the cap design exists to preserve gets one verifier pass and zero confirmed fix iterations. Scenario E dodges it by choosing `maxIterations: 4` (⇒ 2); the default value is never exercised. The disposition's mutation check (`const retryTask = task`) only tests the opposite direction. | Bug — default config hits the degraded path | Correctness |
| 3 | `skills/execute/wave-execute.workflow.js:818` | Rewritten `escalate()` comment asserts a false invariant: "every builder reads `st.capSuppressed` directly, which is what makes the `plannerPrompt` class of omission unwritable". No prompt builder reads `st.capSuppressed` anywhere in the file. | `grep -n capSuppressed` returns only 761 (comment), 818 (this comment), 823-825 (escalate), 860 (effortTier), 1119, 1126 (converge). Builders actually read `st.escalated` (escalationContext:365) and `terminalTier(st)` (verifyBlock:344). The comment is load-bearing — the only record of WHY the round-2 finding-1 omission can't recur — so a maintainer who trusts it greps the builders, finds nothing, and either concludes the safety property was dropped or "restores" it by threading a flag with two working substitutes. The claim about the single live consumer (`implement()`'s `moved`) is correct; only the builder sentence is wrong. | Bug — false load-bearing comment | Correctness / docs-in-code |
| 4 | `skills/execute/tests/prompt-invariants.test.mjs:321` | Scenario E's headline claims it proves "judges clamped", but `aEff` carries no `judgeModel`, so the judge dispatch never exercises `clampTier` — the judge model equals `st.tier` regardless. | Mutation-proven: replacing `judgeFor` with `return normaliseTier(a && a.judgeModel, st.tier)` (clamp removed entirely) fails exactly two assertions — :186 and :64 — and ZERO cap-E assertions. In Scenario E, `judgeFor({}, st) = normaliseTier(undefined,'opus') = 'opus'` with or without the clamp, so `capCalls.every(c => c.model === 'opus')` passes vacuously for the judge path. Exactly the defect class round-2 finding 9 covered, which the disposition table claims closed. Fix: add `judgeModel: 'fable'` to Scenario E's args so the judge dispatch goes through the ceiling. | Bug — vacuous assertion; claimed coverage absent | Test correctness |
| 5 | `skills/execute/tests/prompt-invariants.test.mjs:340` | Assertion label "the two passes SHARE max_iterations" is false — its own values are 4 then 2, i.e. 1.5× `max_iterations`, still MORE verifier time than an uncapped run (1 + n). | With `max_iterations: n`, a capped task spends n (terminal first pass) + floor(n/2) (retry) = 1.5n verifier iterations; uncapped spends 1 + n. For n=8 that's 12 vs 9. Round-2 finding 7 said a capped run consumes 2× the documented budget; this fix reduces it to 1.5× but the test, the disposition ("The doubling is gone") and the template's worst-case formula at rollout-template.md:71 all present it as eliminated. An operator sizing a wave from that formula under a cap under-budgets wall-clock by ~50% on the implement phase, with `parallel_ceiling` tasks running concurrently. | Bug — mislabelled assertion + under-stated budget | Correctness / docs |
| 6 | `skills/execute/tests/prompt-invariants.test.mjs:331` | Scenario E dereferences `capCall(...)` with no null guard, so a label-format change turns a FAIL line into an uncaught TypeError that aborts ~250 remaining assertions — asymmetric with every other scenario in the file. | `capCall` is an exact-match find returning `undefined` on a miss. Lines 331-334 and 337-338 read `.effort`/`.prompt` directly (`capCall('plan:proj-eff-e').effort === 'medium'`). Rename the plan dispatch label to e.g. `planner:${slug}` and the suite throws `Cannot read properties of undefined (reading 'effort')` at :331; node exits non-zero with no `FAIL -` line, and the gated-inputs block, review-loop-memory block and agent-death block never run. Scenarios A-D use the guarded form (`ok(call('x') && call('x').effort === …)`) which degrades to a clean single FAIL. Use the same guard. | Bug — test-harness fragility masks unrelated coverage | Robustness |
| 7 | `skills/execute/tests/prompt-invariants.test.mjs:310` | Scenario E scripts an implement first-pass reply (`escalate: true, blocked: false`) the capped prompt cannot legitimately produce, so the budget-split assertion is validated against an off-contract agent response. | Under `maxTier:'opus'` the first implement pass renders `ralphLoop` (terminalTier true — asserted by the test itself at :339), and `ralphLoop` never mentions `escalate`; step (d) instructs `blocked=true, verified=false`. Only `oneShotVerify` instructs `escalate=true`. The mock returns a signal only a NON-capped first pass could emit. The branch still fires because `implement()` tests `(r.escalate \|\| r.blocked)`, so the test is green — but it documents a contract the engine forbids, and never proves the capped path is reached via the `r.blocked` arm, the only arm a compliant capped agent can take. Return `blocked: true, escalate: false` (verified equivalent by probe). | Bug — test validates an impossible agent contract | Test correctness |
| 8 | `skills/schedule/rollout-template.md:23` | Uncommenting the new `# max_tier:` line without typing a value yields `max_tier: null`, which `tierCap()` treats as ABSENT and silently runs UNCAPPED — the opposite of intent, with no log line. | `tierCap()` fails CLOSED on an unrecognised non-empty value but OPEN on `undefined`/`null`/`''`. An operator in the one condition the key exists for (fable quota exhausted) uncomments the line, sees `max_tier:` present, and launches. The skill passes `maxTier: null` (or omits it), `tierCap` returns `TOP_TIER`, and the startup guard `if (tierCap(a) !== TOP_TIER) log(...)` prints NOTHING — no signal at all. The rollout then escalates freely into the exhausted quota. `env_bootstrap` has the same shape but a benign failure mode; this key's failure mode is spending the resource it protects. Either write the line as `# max_tier: opus` so uncommenting is complete, or make `tierCap` treat present-but-empty as the strict cap. | Bug — silent fail-open on the guard's own trigger condition | Correctness / UX |
| 9 | `skills/execute/tests/prompt-invariants.test.mjs:251` | New `judgeEffort` assertion is strictly redundant with the line directly above — `judgeModel: 'fable'` clamps to `'opus'`, so both compute the identical `pinned === 'opus'`; no mutation can fail one without the other. | `judgeFor({judgeModel:'fable'},{tier:'opus',cap:'opus'}) = clampTier('fable','opus') = 'opus'`, exactly what :250's `judgeModel:'opus'` yields; both take the same `tier = effortTier(st)` branch. Verified: the F6 mutation (`const tier = pinned \|\| effortTier(st)`) fails both; no mutation fails :251 alone. The stated purpose — covering "the reported inversion" verbatim — is documentation value only, and the label overstates it as distinct coverage. If kept, say so in a comment; the genuinely uncovered case is a pinned judge in an end-to-end capped `converge()` run (see finding 4). | Cleanup — redundant assertion, overstated label | Simplification |
| 10 | `skills/schedule/rollout-template.md:26` | Template comment points readers at "execute SKILL.md § Config", a heading that does not exist. | `grep -n '^#\{1,4\} ' skills/execute/SKILL.md` lists no heading containing 'Config'; the config table lives under `### 3. Resolve effective config per task`. This comment is copied verbatim into every generated rollout note in the operator's vault, where they cannot grep the plugin source — so the one pointer they have when the cap misbehaves resolves to nothing. Name the real heading. | Bug — dangling cross-reference propagated to user artefacts | Docs |
| 11 | `skills/schedule/rollout-template.md:23` | Template documents `max_tier: (opus \| fable)`, but `fable` is the uncapped default, so setting it is a no-op — self-contradictory with the next clause, "set ONLY when the fable quota is exhausted". | `TOP_TIER` is `'fable'` and `tierCap` returns `'fable'` when absent, so `max_tier: fable` produces a byte-identical uncapped run. An operator following the stated trigger who reads `(opus \| fable)` as a menu has a 50% chance of picking the value that does nothing, with no warning — the startup guard only logs when `tierCap(a) !== TOP_TIER`. The only meaningful value today is `opus`; say so. (execute/SKILL.md:83 carries the same wording, so the diff propagates an existing wart into a user-facing artefact.) | Bug — misleading documented option set | Docs / UX |
| 12 | `skills/execute/tests/prompt-invariants.test.mjs:298` | Scenario E re-implements the file's existing `recordingAgent`/`call` helpers as `capCalls`/`capCall` with silently DIFFERENT matching semantics (exact `===` vs `startsWith`), side by side in one file. | `call` (:137) is `effortCalls.find(c => c.label.startsWith(label))`; `capCall` (:318) is `capCalls.find(c => c.label === label)`. A maintainer copying an assertion between scenarios — e.g. moving `call('review:proj-eff-b')` into Scenario E — silently gets a miss, because the review label is `review:<slug> r1` and only the startsWith form matches. Scenario E also bypasses `recordingAgent` entirely, so `effortCalls` silently retains Scenario D's records for all of Scenario E. The only reason a bespoke recorder was needed is that `recordingAgent` does not capture `prompt`; add `prompt` to `recordingAgent` and Scenario E can use the existing helpers. | Cleanup — duplicated helper with divergent semantics | Simplification |

## How this review ran

- Tree reviewed: `/Users/lachlants/repos/tools/thread-skill` @ `07ddc75`, uncommitted diff of the
  4 modified files (digest `e55ab3`), plus `THREAD.md` in scope.
- Harness/engine: `claude` / built-in `code-review`, mode `code`, effort `xhigh`.
- The engine forked into a background `code-review` agent. The wrapper observed exactly 1 spawn
  (the fork) and could not count any fan-out beneath it, so **no multi-agent fan-out is claimed**.
  It did not degrade to the no-Agent-tool single pass.
- No findings landed on `THREAD.md` or `skills/schedule/SKILL.md`.
