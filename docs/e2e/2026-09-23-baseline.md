---
checklist: thread plugin live end-to-end
plugin: 2.5.3
status: running        # pending → running → recorded
fixture_repo: ~/repos/tools/zz-thread-e2e (GitHub lachyts/zz-thread-e2e, private, master only)
fixture_project: "[[ZZ Thread E2E]]"  # ~/repos/obsidian/Work/Projects/Tools/ZZ Thread E2E.md
---

# Live end-to-end baseline — 2026-09-23

Layer 3 of the big test (the audit, `docs/audits/2026-09-23-thread-audit.md`): every verb run for
real against throwaway fixtures, once, on the 2.5.3 plugin. The same checklist is the **2.6.0 release
gate** after the self-rollout. Record each row as `PASS`, `FAIL — <what happened>` or
`KNOWN — <defect it hit>`; every FAIL becomes a vault task (task-writer, with dedup) before
`/thread:gather` runs.

## 0. Setup (S1, before any verb)

- [x] Fresh session on 2.5.3+: `claude plugin list` shows `thread@thread` 2.5.3 at user scope (a stale
      2.3.4 project-scope record at `~` may still be listed — never launch from `~`), `make -C
      ~/repos/tools/thread-skill release-check` passes, and the cached engine contains `defaultBranch`.
- [x] Leak baseline: `date +%s` → STAMP; HEADs of `~/.claude`, `~/repos/workspaces`, `~/repos/obsidian`.
- [x] Fixture repo `~/repos/tools/zz-thread-e2e`: `git init -b master`; `a.txt`, `b.txt`; a `Makefile`
      whose `test` target checks both files exist and contain no `FAIL` line; commit; then
      `gh repo create lachyts/zz-thread-e2e --private --source . --push`. Verify there is no `main`
      anywhere: `git ls-remote --heads origin` lists `master` only.
- [x] Vault fixture: `Work/Projects/Tools/ZZ Thread E2E.md` (project note, `repos: [~/repos/tools/zz-thread-e2e]`)
      and tasks `zz-thread-e2e-t1` (append a line to a.txt), `-t2` (append to b.txt), `-t3` (append a
      second line to a.txt, `depends-on: [[zz-thread-e2e-t1]]`), `-cms` (publish a.txt to a CMS page —
      the misfit). All `status: open`, `projects: ["[[ZZ Thread E2E]]"]`, each with a verify line.
- [ ] Launch the session with cwd = the fixture repo. This takes a **new** session: the harness resets any Bash `cd` outside the launch directory, so the § 0 session can't move there itself.

**§ 0 record (S1, 2026-09-23):**

- Plugin: `thread@thread` 2.5.3 at user scope; the stale 2.3.4 project-scope record is still listed. `release-check` passes, and the cached `wave-execute.workflow.js` and `merge-wave.sh` contain `defaultBranch`.
- The 2.5.3 cache holds a stale `.claude/worktrees/enabler/` copy (912K) that the install copied from the working tree. It is not engine-affecting (see § 5).
- STAMP `1790150927` (18:08:47 AEST). Heads: `~/.claude` `eee2821`, `~/repos/workspaces` `5ac0113`, `~/repos/obsidian` `8b8852a`.
- Porcelain snapshots at STAMP: 9, 2 and 71 dirty entries. The obsidian repo already had 71 dirty entries before S1, and three in-flight rollouts write to it, so the leak check diffs against this snapshot rather than a clean tree.
- Fixture `fec863e` became `ba48589` after I amended out a `.gitignore`, keeping it to spec so `.claude/worktrees/` stays untracked, as in a real repo. `make test` positive and negative checks both pass. `ls-remote` shows `master` only, the GitHub default is `master`, and there is no local `origin/HEAD`.
- **Known post-STAMP write:** the setup session saved one memory file after STAMP, `harness-cd-reset-outside-launch-dir.md`, in the thread-skill project memory dir (plus its `MEMORY.md` index line). It is deliberate; exclude it from § 3's memory check.
- **Launch directory:** S1 was launched in `~/repos/tools/thread-skill`, because the fixture didn't exist yet. A Bash `cd` into the fixture is reset by the harness (`Shell cwd was reset to …/thread-skill`), so the session can't move there. The CWD-bound verbs, 13 in particular (handoff `<home>` is the CWD's git toplevel), need a session launched in the fixture repo.

## 1. Session S1 — verbs 1–13

| # | Verb | Pass criteria | Result |
|---|---|---|---|
| 1 | `/thread:open` | Creates `~/repos/workspaces/_shared/threads/zz-thread-e2e.md` (open's rule for work outside `~/Projects`) | |
| 2 | `/thread:gather ZZ Thread E2E --light` | Phase note `Work/Phases/zz-thread-e2e-p1-*`; t1–t3 renamed `zz-thread-e2e-p1-M-*` with backlinks rewritten; `-cms` left loose; no `wave:` stamped; no CONTEXT.md written | |
| 3 | `/thread:split` a two-line plan | A phased task with a runnable prompt and a verify line (task-writer shape) | |
| 4 | `/thread:schedule ZZ Thread E2E` | Execution-fit names `-cms` a misfit; dated `zz-thread-e2e-rollout-2026-09-2x` note, `protocol_version: 3`; verifier `make test`; waves {t1, t2} → {t3}; `## File-sets` lists a.txt/b.txt | |
| 5 | `/thread:execute [[rollout]]` (continuous) | **The default-branch proof:** the launch resolves `defaultBranch: master`; worktrees under `.claude/worktrees/` branch from `origin/master`; PRs target `master`; `merge-wave.sh` logs `local master fast-forwarded to origin/master.`; t3's branch contains t1's merge (`git merge-base --is-ancestor <t1 merge> <t3 head>`) | |
| 6 | `/thread:status` (during wave 2) | Cursor and per-task state correct; no vault writes (`git -C ~/repos/obsidian status` unchanged by it) | |
| 7 | `/thread:repair` | After hand-flipping t2 back to `in_progress`, it resolves t2 to done from the merged PR with no re-dispatch | |
| 8 | `/thread:next` | Exactly one recommendation, dispatched to a sibling | |
| 9 | `/thread:orient ZZ Thread E2E` | Audit + one recommendation; `-cms` routed to the session lane; choose **Hands-on** (no batches launched) | |
| 10 | `/thread:stash` | A self-contained task: no `scheduled:`, a resume prompt, an NTFS-legal filename, routed to `[[ZZ Thread E2E]]` | |
| 11 | `/thread:open [[<stash task>]]` | Resumes; the capture task auto-completes (`status: done`) | |
| 12 | `/thread:defer friday` | `scheduled:` = the next Friday computed independently (`TZ=Australia/Melbourne`); the Day note carries its `## To do` line (task-writer § 3b) | |
| 13 | `/thread:handoff` | `docs/handoffs/2026-09-2x-<slug>.md` committed in the fixture repo, `status: pending`; labelled a manual handoff; absolute path in the prompt | |

## 2. Session S2 — verbs 14–15 (cold pickup)

| # | Verb | Pass criteria | Result |
|---|---|---|---|
| 14 | `/thread:open <absolute doc path>` | Doc marked `consumed` | |
| 15 | `/thread:close` | Thread file updated; the consumed doc `git rm -f`'d and committed; memory triage all NOOP (no new memory files); decline the vault-task question | |

## 3. Leak check

- [ ] `git -C <repo> log --since=@STAMP --name-only` and `git -C <repo> status` for `~/.claude`,
      `~/repos/workspaces`, `~/repos/obsidian`: every path touched is a fixture path (the thread
      file, the fixture Day-note line, fixture tasks/phase/project). Anything else → revert by path.
- [ ] No new file under `~/.claude/memory/` or the project memory dirs since STAMP.

## 4. Cleanup

- [ ] Remove fixture worktrees (`git -C ~/repos/tools/zz-thread-e2e worktree list`).
- [ ] `gh repo delete lachyts/zz-thread-e2e --yes`.
- [ ] `git rm` the vault fixtures (Tasks incl. Archive, Phases, the project note) and remove the
      Day-note `## To do` line; remove `_shared/threads/zz-thread-e2e.md` (+ its INDEX line).
- [ ] Clear the fixture sessions' `~/.claude/wave-driver/<session>.json`.
- [ ] `rm -rf ~/repos/tools/zz-thread-e2e` (last).
- [ ] Verify: `grep -rl zz-thread-e2e ~/repos/obsidian ~/repos/workspaces ~/.claude/memory` → nothing;
      `gh repo view lachyts/zz-thread-e2e` → not found.
- [ ] Verb 5 passed → close vault task `thread-plugin-default-branch-fix`.

## 5. Findings

(one line per FAIL/KNOWN, with the vault task it became)

- **§ 0, release hygiene (not a verb):**
  - What it is: the 2.5.3 and 2.5.2 caches carry `.claude/worktrees/enabler/`. It is a full nested plugin copy (`.claude-plugin/`, `skills/`, a `.git` file pointing at a deleted worktree gitdir), which the install copied from the git-ignored working-tree directory.
  - Why it wasn't caught: `release-check` compares only `skills/` and `hooks/`, so it passes.
  - Impact: none observed. The skill listing shows no duplicate skills.
  - Task: S2 files it with the other findings. The fix is for `release-check` to flag cache content outside the tracked tree, or for the release to refuse while worktrees exist.
- **§ 0, checklist shape (not a plugin defect):** the harness resets a Bash `cd` outside the launch directory, so § 0 and verbs 1–13 can't share a session. S1 is therefore split: the setup session launched in thread-skill, then a verbs session launched in the fixture. The § 0 wording above now says so, ready for the 2.6.0 re-run.
